arch-image: $(IMAGE)

$(IMAGE): $(INITRD)
ifeq ($(TYPE),netboot)
	set -e; $(foreach NAME,$(KERNELNAME), \
		tftpboot.sh $(TEMP)/$(NAME) /dev/null $< $@;)
endif
ifeq ($(TYPE),cdrom)
	set -e; $(foreach NAME,$(KERNELNAME), \
		tftpboot.sh $(TEMP)/$(NAME) /dev/null $< $@;)
endif
