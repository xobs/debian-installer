# Note: This modulemust have loop.o in it.
floppy-modules-${kernel:Version}-udeb
busybox-cvs-floppy-udeb
# Include support for USB floppy drives.
#usb-modules-${kernel:Version}-udeb
#usb-storage-modules-${kernel:Version}-udeb
#usb-discover
# Argh. This is here for *one symbol*!! And it makes the floppy 55k too
# big. Herbert promises to fix it.
#ide-modules-${kernel:Version}-udeb
