# These udebs will be needed on every debian-installer system.
busybox-udeb
dash-udeb
rootskel
cdebconf-udeb
main-menu
udpkg
anna
# The di-utils-shell package depend on the old and missin libdebconf.so.1.
# This will be fixed by the next upload [pere 2003-05-29]
#di-utils-shell
# The kernel is just unpacked to copy the source.  It is not included
# in the initrd file.
kernel-image-${kernel:Version}-udeb
