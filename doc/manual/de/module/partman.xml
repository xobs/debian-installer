<!-- original version: 14559 -->

   <sect2 id="partman"><title>Partitionierung Ihrer Platten</title>
<para>

Nun ist es Zeit, Ihre Platten zu partitionieren. Wenn sich mit
Paritionierung nicht auskennen, oder einfach mehr Details wissen
wollen, siehe <xref linkend="partitioning"/>.

</para><para>

Zuerst wird Ihnen die Auswahl geboten, automatisch entweder eine gesamte Platte
zu partitionieren, oder nur den freien Speicherplatz auf einer Platte
zu verwenden. Wenn Sie keine automatische Partitionierung wollen, wählen Sie
<quimenuitem>Partitionierungstabelle von Hand eingeben</quimenuitem> aus dem
Menü und überspringen Sie den nächsten Absatz. Ansonsten werden Sie gefragt,
ob Sie <quimenuitem>Alle Dateien auf eine Partition</quimenuitem> kopieren
wollen, oder ob Sie lieber eine <quimenuitem>Separate Partition für die
Home-Verzeichnisse</quimenuitem> wollen. Beide Einstellungen haben ihre
Vor- und Nachteile, von denen einige in <xref linkend="partitioning"/>
behandelt werden. Wenn Sie unsicher sind, wählen Sie ersteres.

</para><para>

Auf dem nächsten Schirm sehen Sie Ihre Partitionstabelle, wie die Partitionen
formatiert werden, und wo sie eingehängt werden. Wenn Sie die automatische
Partitionierung durchgeführt haben, sollten Sie nur
<quimenuitem>Finished partitioning</quimenuitem> zur Auswahl haben,
um zu verwenden, was eingestellt wurde.

</para><para>

Wählen Sie Partitionen aus der Liste, um sie zu modifizieren oder zu löschen.
Wenn Sie freien Speicherplatz haben, wird er ebenfalls unter einer Platte
aufgelistet sein, und Sie können ihn auswählen, um neue Partitionen zu erzeugen.
Beim Ändern einer Partition haben Sie die Auswahlmöglichkeit, welches
Dateisystem verwendet werden soll, und die Partition gemountet werden soll.
Das Partitionierungsmenü bietet unten auch eine Auswahl, eine Platte
oder den darauf befindlichen freien Speicherplatz automatisch zu partitionieren,
wenn Ihnen das lieber ist.

Select partititons from the list to modify or delete them. If you have
free space it will also show up under a drive, and you can select it
to create new partitions. When modifying a partition you will have the
opportunity to choose the file system to use, and where to mount
it. The partitioning menu also has a choice at the bottom that can be
used to automatically partition a drive or existing free space on
a drive, if you'd rather go that route. Be sure to create at least two
partitions, one for <emphasis>swap</emphasis> and one for the
<emphasis>root</emphasis> filesystem (which must be mounted as
<filename>/</filename>). If you forget to mount the root filesystem,
<command>partman</command> won't let you continue until you correct
this issue.

</para><para>

Capabilities of <command>partman</command> can be extended with
installer modules, so if you can't see all promised goodies, check if
you have loaded all required modules
(e.g. <filename>partman-ext3</filename>,
<filename>partman-xfs</filename>, or
<filename>partman-lvm</filename>).

</para><para>

After finishing partitioning, select <guimenuitem>Finished
partitioning</guimenuitem> from the menu, and confirm that the
filesystems should be created as requested.

</para>
   </sect2>
