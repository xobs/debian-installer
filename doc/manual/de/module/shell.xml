<!-- original version: 14559 -->

   <sect2 id="shell">
<title>Verwenden der Shell und Ansehen der Logs</title>

<para>

Es gibt einen Menüpunkt Eine Shell ausführen im Menü. Wenn das Menü nicht
verfügbar ist und Sie trotzdem eine Shell benötigen, drücken Sie
<keycombo><keycap>Linke Alt-Taste</keycap> <keycap>F2</keycap></keycombo>
(auf einem Mac Keyboard <keycombo><keycap>Option</keycap> <keycap>F2</keycap>
</keycombo>), um zur zweiten <emphasis>virtuellen Konsole</emphasis> zu
gelangen. Das ist die <keycap>Alt-Taste</keycap> links von der
<keycap>Leertaste</keycap>, und die <keycap>F2</keycap> Funktionstaste, zur
gleichen Zeit. Das ist ein separates Fenster, in dem <command>ash</command>,
ein Bourne-Shell-Klon läuft.

</para><para>

Zu diesem Zeitpunkt ist das System von der RAM Disk gestartet, und eine
eingeschränkte Auswahl an Unix-Werkzeugen sind verfügbar. Sie können sich
ansehen, welche Programme verfügbar sind, indem Sie das Kommando
<userinput>ls /bin /sbin /usr/bin /usr/sbin</userinput> ausführen. Der
Text-Editor ist <command>nano</command>. Die Shell hat einige nette
Eigenschaften, wie Autocompletion und Befehls-History.

</para><para>

Verwenden Sie die Menüs, um die Aufgaben zu bewältigen, die damit möglich
sind &mdash; die Shell und die Kommandos sind nur für den Fall da, dass etwas
fehlschlägt. Im speziellen sollten Sie immer das Menü und nicht die Shell
verwenden, um Ihre Swap-Partition zu aktivieren, da die Menü-Software nicht
erkennen kann, dass Sie das von der Shell aus gemacht haben. Drücken Sie
<keycombo><keycap>Linke Alt-Taste</keycap> <keycap>F1</keycap></keycombo>,
um zu den Menüs zurück zu gelangen, oder geben Sie <userinput>exit</userinput>
ein, wenn Sie das Menü zum Aufrufen der Shell benützt haben.

</para>
  </sect2>
