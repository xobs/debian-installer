<!-- original version: 21994 -->

  <sect2 arch="sparc"><title>Booten per NFS</title>
<para>

Um das System per NFS zu installieren, wählen Sie einfach 
<userinput>NFS</userinput> als Ort für die Images und Dateien aus 
und folgen Sie den Instruktionen, die Ihnen angeboten werden.
Sie werden nach dem <filename>server:/directory</filename> gefragt, 
wo die Images liegen.

</para>
  </sect2>


  <sect2 arch="sparc" id="boot-tftp"><title>Booten per TFTP</title>

&boot-installer-intro-net.xml;

<para>

Auf Systemen mit OpenBoot gehen Sie in den Boot-Monitor auf der 
Maschine, die installiert werden soll 
(siehe <xref linkend="invoking-openboot"/>).
Nutzen Sie <userinput>boot net</userinput>, um von einem 
TFTP-RARP-Server zu booten; Sie können auch 
<userinput>boot net:bootp</userinput> oder 
<userinput>boot net:dhcp</userinput> versuchen, um von einem 
TFTP-BOOTP- oder DHCP-Server zu booten. Einige ältere Versionen von 
OpenBoot benötigen auch den Gerätenamen im Boot-Kommando, wie z.B. 
<userinput>boot le()</userinput>; diese unterstützen unter Umständen 
weder BOOTP noch DHCP.

</para>
  </sect2>


  <sect2 arch="sparc"><title>Booten von einer CD-ROM</title>

&boot-installer-intro-cd.xml;

<para>

Die meisten OpenBoot-Versionen unterstützen das 
<userinput>boot cdrom</userinput>-Kommando, das einfach nur ein Alias 
ist für das Booten vom SCSI-Gerät mit der ID 6 (oder vom Master des 
zweiten IDE-Kanals bei IDE-Systemen). Bei älteren OpenBoot-Versionen, 
die diese spezielle Kommando noch nicht unterstützen, 
müssen Sie manchmal den Gerätenamen direkt verwenden. 
Beachten Sie, dass auf Sun4m-Systemen (z.B. Sparc 10s und Sparc 20s) 
von Problemen berichtet wurde im Zusammenhang mit dem Booten von CD-ROM.

</para>
  </sect2>


  <sect2 arch="sparc">
  <title>Booten von Floppy-Disks</title>
<para>

Um ein Sparc-System von einer Floppy-Diskette zu booten, benutzen Sie 
<informalexample><screen>

Stop-A -> OpenBoot: "boot floppy"

</screen></informalexample>
</para><para>

Seien Sie gewarnt, dass neuere Systeme der Sun4u-(ultra-)Architektur das
Booten von Floppy nicht unterstützen. Eine typische Fehlermeldung ist:
<computeroutput>Bad magic number in disk label - Can't open disk 
label package</computeroutput>. Außerdem unterstützt eine Reihe von 
Sun4c-Modellen (wie der IPX) die komprimierten Images auf den Disks nicht, 
also wird auch hier das Booten per Floppy nicht unterstützt.

</para><para>

Mehrere Sparc-Systeme (z.B. Ultra 10) haben einen OBP-Fehler, der Sie 
daran hindert, Debian zu booten (anders als das Problem, dass das 
Booten von Debian überhaupt nicht unterstützt wird). Das nötige OBP-Update 
kann unter der Produkt-ID 106121 von <ulink url="http://sunsolve.sun.com"></ulink> 
heruntergeladen werden.

</para><para>

Wenn Sie von Floppy-Disk starten und Meldungen wie diese sehen:
<informalexample><screen>

Fatal error: Cannot read partition
Illegal or malformed device name

</screen></informalexample>
ist es möglich, dass das Booten von Floppy von Ihrem Rechner einfach 
nicht unterstützt wird.

</para>
  </sect2>

  <sect2 arch="sparc"><title>IDPROM-Meldungen</title>
<para>

Wenn Sie nicht booten können und Meldungen über ein Problem mit
``IDPROM'' erhalten, ist möglicherweise die NVRAM-Batterie leer, 
die die Konfigurationsdaten der Firmware sichert. Lesen Sie 
die <ulink url="&url-sun-nvram-faq;">Sun NVRAM FAQ</ulink>, wenn Sie 
mehr Informationen brauchen.

</para>
  </sect2>
