<!-- retain these comments for translator revision tracking -->
<!-- $Id: mips.xml 22939 2004-10-09 23:27:48Z ths-guest $ -->


  <sect2 arch="mips" id="boot-tftp"><title>Загрузка по TFTP</title>

   <sect3>
   <title>Загрузка по TFTP SGI Indys</title>
<para>После входа в командный монитор используйте <informalexample><screen>

bootp():

</screen></informalexample> на SGI Indys для загрузки linux и начала установки Debian. Чтобы сделать это, вам надо очистить переменную окружения <envar>netaddr</envar>. Для этого наберите <informalexample><screen>

unsetenv netaddr

</screen></informalexample> в командном мониторе. </para>
  </sect3>

   <sect3>
   <title>Загрузка по TFTP Broadcom BCM91250A</title>
<para>Для Broadcom BCM91250A evaluation по TFTP загружается системный загрузчик SiByl, который затем загружвает и запускается программу установки Debian. В большинстве случаев, сначала вам нужно получить IP адрес по DHCP, но также возможна настроить статический адрес. Чтобы использовать DHCP, вы можете ввести следующую командув строке приглашения CFE: <informalexample><screen>

ifconfig eth0 -auto

</screen></informalexample> После получения IP адреса, вы можете загрузить SiByl с помощью следующей команды: <informalexample><screen>

boot 192.168.1.1:/boot/sibyl

</screen></informalexample> Вместо приведённого в этом примере IP адреса вам нужно указать или имя или IP адреса TFTP сервера. После выполнения этой команды, программа установки будет загружена автоматически. </para>
</sect3>
  </sect2>

  <sect2 arch="mips"><title>Boot Parameters</title>

   <sect3>
   <title>SGI Indys TFTP Booting</title>
<para>

On SGI Indys you can append boot parameters to the
<command>bootp():</command> command in the command monitor.

</para><para>

Following the <command>bootp():</command> command you can give the
path and name of the file to boot if you did not give an explicit name
via your bootp/dhcp server - Example:
<informalexample><screen>

bootp():/boot/tftpboot.img

</screen></informalexample>
</para><para>

Further kernel parameters can be passed via <command>append</command>:
<informalexample><screen>

bootp(): append="root=/dev/sda1"

</screen></informalexample>
</para>
  </sect3>

   <sect3>
   <title>Broadcom BCM91250A TFTP Booting</title>
<para>

You cannot pass any boot parameters directly from the CFE prompt.  Instead,
you have to edit the <filename>/boot/sibyl.conf</filename> file on the TFTP
server and add your parameters to the <replaceable>extra_args</replaceable>
variable.

</para>
  </sect3>

  </sect2>
