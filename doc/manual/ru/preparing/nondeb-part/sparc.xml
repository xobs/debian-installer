<!-- retain these comments for translator revision tracking -->
<!-- $Id: sparc.xml 11648 2004-03-22 00:37:46Z joeyh $ -->


  <sect2 arch="sparc"><title>Разметка из SunOS</title>

<para>Это замечательно делается из SunOS; фактически, если вы намереваетесь работать и в SunOS и в Debian на одной машине, то даже рекомендуется делать разметку с помощью SunOS перед установкой Debian. Ядро Linux понимает таблицу разделов Sun (disk labels), поэтому с этим не будет никаких проблем. Только проверьте, что разместили корневой раздел Debian в пределах первого гигабайта загрузочного диска. Также вы можете положить образ ядра в UFS раздел, если это легче чем создавать корневой раздел. SILO поддерживает загрузку Linux и SunOS с разделов EXT2 (Linux), UFS (SunOS), romfs и iso9660 (CDROM). </para>
  </sect2>

  <sect2 arch="sparc"><title>Partitioning from Linux or another OS</title>

<para>

Whatever system you are using to partition, make sure you create a
``Sun disk label'' on your boot disk.  This is the only kind of
partition scheme that the OpenBoot PROM understands, and so it's the
only scheme from which you can boot.  In <command>fdisk</command>, the
<keycap>s</keycap> key is used to create Sun disk labels. You only need to do this
on drives that do not already have a Sun disk label. If you are using a
drive that was previously formatted using a PC (or other architecture) you
must create a new disk label, or problems with the disk geometry will most
likely occur.

</para><para>

You will probably be using <command>SILO</command> as your boot loader (the
small program which runs the operating system kernel).
<command>SILO</command> has certain requirements for partition sizes and
location; see <xref linkend="partitioning"/>.  

</para>
  </sect2>
