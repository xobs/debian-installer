<!-- retain these comments for translator revision tracking -->
<!-- $Id: apt.xml 18642 2004-07-27 22:06:42Z joeyh $ -->

 <sect2 id="configure-apt">
 <title>Настройка APT</title>

<para>В основном люди для установки пакетов на своей системе используют программу под названием <command>apt-get</command> из пакета <classname>apt</classname>.<footnote> <para> В действительности, программа, которая устанавливает пакеты, называется <command>dpkg</command>. Однако, этот пакет более низкоуровневая утилита. <command>apt-get</command> вызывает <command>dpkg</command> при необходимости; это утилита более высокого уровня, поскольку знает, как установить другие пакеты, которые требуются пакету, который вы ходите установить, а также знает, как запросить пакет с вашего компакт-диска, по сети, или как-то еще. </para> </footnote> APT должен быть настроен, чтобы он знал, откуда запрашивать пакеты. С этой задачей поможет справиться программа <command>apt-setup</command>. </para><para>Следующим шагом в процессе настройке является указание APT, где можено найти остальные пакеты Debian. Помните, что вы можете запустить эту утилиту снова в любой момент после установки командой <command>apt-setup</command> или вручную отредактировать <filename>/etc/apt/sources.list</filename>. </para><para>Если в это момент в дисководе есть официальный компакт-диск, то этот диск должен автоматически настроиться в качестве источника apt безо всяких подсказок. Вы заметите это, потому что увидите, как сканируется диск. </para><para>Для пользователей без официального компакт-диска будет предложен список с выбором метода доступа к пакетам Debian: FTP, HTTP, CD-ROM или локальная файловая система. </para><para>Вы должны знать, что вполне разрешено иметь несколько разных источников APT, даже для одного и того же архива Debian. <command>apt-get</command> автоматически выберет пакет с наибольшим номером версии среди всех доступных версий. Или, например, если у васть есть и HTTP-, и CD-ROM-источники APT, <command>apt-get</command> автоматически использует локальный CD-ROM когда это возможно, и обратится к HTTP только если там есть более новая версия. Однако, добавлять ненужные источники APT это не очень хорошая идея, поскольку это замедлит процесс проверки сетевых архивов на новые версии. </para>

  <sect3 id="configure-apt-net">
  <title>Настройка сетевых источников пакетов</title>

<para>Если вы планируете установить оставшуюся часть системы по сети, наиболее общим вариантом является выбор <userinput>http</userinput> источника. <userinput>ftp</userinput> источник тоже возможен, но приведет к чуть более медленной установке соединения. </para><para>Следующим шагом при настройке сетевых источников пакетов является указание <command>apt-setup</command>, в какой стране вы живете. Это задает, к какому официальному Internet-зеркалу Debian вы подключитесь.В зависимости от того, какую страну вы выберете, вам будет дан список возможных машин. В общем случае наплохо бы выбрать одну из верхних в списке, хотя любая из них должна работать. </para><para>Если вы устанавливаетесь по HTTP, вас попросят задать прокси-сервер. Это иногда необходимо людям за firewall, в корпоративной сети и т.д. </para><para>И наконец, ваш сетевой источник пакетов будет проверен. Если все пройдет нормально, вам будет предложено повторить все это еще раз с другим сетевым источником. </para>
  </sect3>
 </sect2>
