<!-- retain these comments for translator revision tracking -->
<!-- $Id: dhcp.xml 22664 2004-10-06 15:49:49Z fjpop-guest $ -->


  <sect2 condition="supports-dhcp" id="dhcpd">
   <title>Настройка DHCP сервера</title>
<para>Есть один свободный DHCP сервер: это ISC <command>dhcpd</command>. В &debian;, он доступен из пакета <classname>dhcp</classname>. Вот пример его конфигурационного файла (обычно <filename>/etc/dhcpd.conf</filename>): <informalexample><screen>

option domain-name "example.com";
option domain-name-servers ns1.example.com;
option subnet-mask 255.255.255.0;
default-lease-time 600;
max-lease-time 7200;
server-name "servername";

subnet 192.168.1.0 netmask 255.255.255.0 {
  range 192.168.1.200 192.168.1.253;
  option routers 192.168.1.1;
}

host clientname {
  filename "/tftpboot/tftpboot.img";
  server-name "servername";
  next-server servername;
  hardware ethernet 01:23:45:67:89:AB; 
  fixed-address 192.168.1.90;
}

</screen></informalexample> Примечание: новый (и предпочтительный) пакет <classname>dhcp3</classname> использует <filename>/etc/dhcp3/dhcpd.conf</filename>. </para><para>В этом примере определён единственный сервер <replaceable>"servername"</replaceable>, который выполняет всю работу DHCP, TFTP серверов и шлюза сети. Вам почти наверняка нужно изменить опцию domain-name, а также server-name и аппаратный адрес клиента. Опция <replaceable>"filename"</replaceable> должна содержать имя файла, который нужно получить по TFTP. </para><para>После редактирования конфигурационного файла <command>dhcpd</command>, перезагрузите его командой <userinput>/etc/init.d/dhcpd restart</userinput>. </para>

   <sect3 arch="i386">
   <title>Включение загрузки PXE в конфигурацию DHCP</title>
<para>Вот другой пример <filename>dhcp.conf</filename>, в котором используется метод предстартового выполнения среды (PXE) по TFTP. <informalexample><screen>

option domain-name "example.com";

default-lease-time 6048;
max-lease-time 604800;

allow booting;
allow bootp;

# The next paragraph needs to be modified to fit your case
subnet 192.168.1.0 netmask 255.255.255.0 {
  range 192.168.1.200 192.168.1.253;
  option subnet-mask 255.255.255.0;
  option broadcast-address 192.168.1.255;
# the gateway address which can be different 
# (access to the internet for instance)
  option routers 192.168.1.1;
# indicate the dns you want to use
  option domain-name-servers 192.168.1.3;
}

host tftpserver {
# tftp server ip address
  fixed-address 192.168.1.90;
# tftp server hardware address
  hardware ethernet 01:23:45:67:89:AB;
}

group {
 next-server 192.168.1.3;
 host tftpclient {
# tftp client hardware address
  hardware ethernet  00:10:DC:27:6C:15;
  filename "/tftpboot/pxelinux.0";
 }
}

</screen></informalexample> Заметим, что для PXE загрузки, в filename клиента<filename>pxelinux.0</filename> записано имя системного загрузчика, а не образа ядра (смотрите ниже <xref linkend="tftp-images"/>). </para>
   </sect3>
  </sect2>
