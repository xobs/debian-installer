<!-- retain these comments for translator revision tracking -->
<!-- $Id: rarp.xml 15652 2004-05-19 00:37:43Z kraai $ -->


  <sect2 condition="supports-rarp" id="tftp-rarp">
   <title>Настройка RARP сервера</title>
<para>Для настройки RARP, вам нужно знать Ethernet адрес (так называемый MAC адрес) клиентского компьютера, на который будет выполняться установка. Если вы не знаете эту информацию, то можете <phrase arch="sparc"> получить её из сообщений начальной загрузкиOpenPROM, воспользоваться командой OpenBoot <userinput>.enet-addr</userinput>, или </phrase> загрузиться в ``Rescue'' режиме (например, со спасательной дискеты) и выполнить команду <userinput>/sbin/ifconfig eth0</userinput>. </para><para>Если система с RARP сервером использует ядро Linux 2.2.x, то вам необходимо заполнитьRARP таблицу ядра. Чтобы сделать это, выполните следующие команды: <informalexample>
<para>

<userinput>/sbin/rarp -s <replaceable>client-hostname</replaceable> <replaceable>client-enet-addr</replaceable> </userinput>

</para><para>

<userinput>/usr/sbin/arp -s <replaceable>client-ip</replaceable> <replaceable>client-enet-addr</replaceable> </userinput>

</para></informalexample>

</para><para>Если вы получили <informalexample><screen>
SIOCSRARP: Invalid argument
</screen></informalexample>, то вероятно, вам нужно загрузить модуль ядра RARP или пересобрать ядро с поддержкой RARP. Попробуйте выполнить <userinput>modprobe rarp</userinput> и затем ввести команду <command>rarp</command> ещё раз. </para><para>В системе с RARP сервером использующей ядро Linux 2.4.x, нет RARP модуля, а вместо него вы должны использовать программу <command>rarpd</command>. Процедура очень похожа на ту, которая используется в SunOS из следующего параграфа. </para><para>В SunOS, вам нужно проверить, что аппаратный адрес Ethernet клиента содержится вбазе данных ``ethers'' (или в файле <filename>/etc/ethers</filename>, или через NIS/NIS+) и базе данных ``hosts''. Затем, вам нужно запустить демона RARP. В SunOS 4, выполните команду (от суперпользователя): <userinput>/usr/etc/rarpd -a</userinput>; в SunOS 5 используйте <userinput>/usr/sbin/rarpd -a</userinput>. </para>
  </sect2>
