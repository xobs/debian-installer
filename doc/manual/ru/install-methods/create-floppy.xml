<!-- retain these comments for translator revision tracking -->
<!-- $Id: create-floppy.xml 18623 2004-07-27 19:08:34Z joeyh $ -->

 <sect1 condition="supports-floppy-boot" id="create-floppy">
 <title>Создание дискет из образов</title>
<para>Загрузочные дискеты обычно используются как последний носитель с которого можно загрузить программу установки на железе, которое не умеет загружаться с CD или каким-то другим способом. </para><para arch="powerpc">Загрузка с дискет не работает в дисководах Mac USB. </para><para arch="m68k">Загрузка с дискет не поддерживается на Amiga или 68k Mac. </para><para>Образы дисков это файлы, содержащие полный слепок дискеты в<emphasis>сыром</emphasis> виде. Образы дисков, такие как <filename>boot.img</filename>, нельзя просто скопировать на дискету. Для этого нужно пользоваться специальной программой, которая пишет образы файлов на дискету в <emphasis>сыром</emphasis> режиме. Это обязательное условие, так как образы представляют собой сырое представление диска; требуется выполнение <emphasis>посекторного копирования</emphasis> данных из файла на дискету. </para><para>Есть несколько техник по созданию дискет из образов файлов, которые зависят от платформы. Этот раздел опишет, как создать дискеты из образов дисков на различных платформах. </para><para>Неважно каким методом вы создадите дискеты, вы только должны помнить о переключении защиты от записи на дискетах после их создания, чтобы непреднамеренно не повредить их. </para>

  <sect2><title>Запись образов дисков из систем Linux или Unix</title>
<para>Для записи дисковых образов файлов на дискеты, вам вероятно понадобятся права суперпользователя. Вставьте хорошую, чистую дискету в дисковод. Затем, используйте команду <informalexample><screen>
dd if=<replaceable>файл</replaceable> of=/dev/fd0 bs=1024 conv=sync ; sync
</screen></informalexample> где <replaceable>файл</replaceable> это один файловых образов дискет (смотрите в <xref linkend="downloading-files"/> какой<replaceable>файл</replaceable> должен быть). <filename>/dev/fd0</filename> это распространённое имя дисковода, оно может быть отличаться от используемого на вашей машине <phrase arch="sparc">(в Solaris это<filename>/dev/fd/0</filename>)</phrase>. Команда может завершиться раньше, чемUnix завершит запись на дискету, поэтому следите за лампой дисковода и перед вытаскиванием дискеты убедитесь, что она погасла и диск перестал вращаться. На некоторых систмах, нужно выдать команду вытаскивания дискеты из дисковода<phrase arch="sparc">(в Solaris используется <command>eject</command>, смотрите страницу руководства)</phrase>. </para><para>Некоторые системы пытаются автоматически монтировать дискету после появления её в дисководе. Вам может потребоваться выключить это свойство, иначе рабочая станция не позволит вам запись на дискету в <emphasis>сыром режиме</emphasis>. К сожалению, как этого достичь зависит от операционной системы. <phrase arch="sparc"> В Solaris лдя получения сырого доступа к дискете вы можете воспользоваться управлением тома. Во-первых, убедитесь, что дискета смонтирована автоматически (с помощью команды <command>volcheck</command> и её эквивалентом из менеджера файлов). Затем, воспользуйтесь командой <command>dd</command> как было показано выше, просто заменив <filename>/dev/fd0</filename> на <filename>/vol/rdsk/<replaceable>floppy_name</replaceable></filename>, где<replaceable>floppy_name</replaceable> это имя дисковода, которое было задано при форматировании (безимянные дискеты имеют имя по умолчанию<filename>unnamed_floppy</filename>).По другим системам, спросите вашего системного администратора. </phrase> </para><para arch="powerpc">Если запись производится на powerpc Linux, то вам нужно извлечь её. Программа<command>eject</command> отлично справляется этим; вам может потребоваться её установка. </para>

  </sect2>
&floppy-i386.xml; &floppy-m68k.xml; &floppy-powerpc.xml; </sect1>

