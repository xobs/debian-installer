<!-- retain these comments for translator revision tracking -->
<!-- $Id: sparc.xml 22935 2004-10-09 18:36:20Z fjpop-guest $ -->


  <sect2 arch="sparc" id="sparc-cpus"><title>Поддержка процессоров, материнских плат и видеокарт</title>
<para>На данный момент перенос на платформу <emphasis>&architecture;</emphasis> поддерживает несколько типов систем Sparc. Системы Sparc делятся на следующие типы: sun4, sun4c, sun4m, sun4d и sun4u. Сейчас мы не поддерживаем очень старое оборудование sun4. Все остальные системы поддерживаются. Sun4d тестировался меньше всего, поэтому возможны проблемы со стабильной работой ядра. Sun4c и Sun4m, наиболее распространённые из старых машин Sparc, включают такие системы, как SparcStation 1, 1+, IPC, IPX и SparcStation LX, 5, 10 и 20 соответственно. Класс систем UltraSparc относится к типу sun4u и поддерживается набором образов установки sun4u. Некоторые системы, относящиеся к поддерживаемым типам, тем не менее, сами не поддерживаются. Это известно про мультикомпьютер AP1000 и Tadpole Sparcbook 1. См. полную информацию в <ulink url="&url-sparc-linux-faq;">FAQ по Linux на процессорах SPARC</ulink>. </para>

   <sect3><title>Настройка памяти</title>
<para>На некоторых старых рабочих станциях Sun, особенно Sun IPX и Sun IPC, банки памяти имеют фиксированные физические адреса. Таким образом, если заполнены не все банки, в физическом адресном пространстве будут пропуски. Установка Linux требует наличия непрерывного блока памяти, в который загружается ядро и начальный диск памяти. Если это невозможно, происходит ошибка Data Access Exception. </para><para>Поэтому вы должны настроить память таким образом, чтобы низший блок памяти бы непрерывен на протяжении хотя бы 8 МБ. В указанных выше системах IPX и IPC границы банков памяти кратны 16 МБ. В результате, это означает, что у вас должен быть модуль SIMM в нулевом банке, достаточно большой, чтобы в нём разместились ядро и диск памяти. 4 МБ для этого <emphasis>недостаточно</emphasis>. </para><para>Пример: На Sun IPX у вас есть два модуля SIMM: 16 МБ и 4 МБ. Есть четыре банка SIMM (0, 1, 2, 3). Нулевой банк - это расположенный дальше всего от коннекторов SBUS.  Таким образом, вы должны установить модуль 16 МБ в банк 0. Модуль размером 4 МБ рекомендуется в этом случае установить в банк 2. </para>
   </sect3>

   <sect3><title>Настройка графики</title>
<para>Достаточно часто эти системы, особенно старые рабочие станции SUN, содержат аппаратный фрэймбуфер на материнской плате (например, bwtwo на Sun IPC) и карту SBUS, содержащую более новый и, вероятно, более быстрый, фрэймбуфер, вставленную в слот SBUS. Под Solaris/SunOS это не вызывает проблем, поскольку инициализируются обе карты. </para><para>Однако, под Linux это может вызвать проблему, состоящую в том, что монитор загрузки PROM может выводить свои сообщения на дополнительную карту, в то время как сообщения ядра Linux могут быть направлены на фрэймбуфер на материнской плате. В этом случае на экране сообщения об ошибках <emphasis>не появятся</emphasis>, и машина, по всей видимости, зависнет при загрузке диска памяти. </para><para>Чтобы избежать этой проблемы, подсоедините монитор (если это нужно) к видеокарте в слоте SBUS с самым маленьким номером (карта на материнской плате считается младшим внешним слотом). В качестве альтернативы, можно использовать последовательную консоль. </para>
   </sect3>
  </sect2>
