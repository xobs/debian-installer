<!-- retain these comments for translator revision tracking -->
<!-- $Id: memory-disk-requirements.xml 19285 2004-08-17 19:12:41Z smarenka $ -->

 <sect1 id="memory-disk-requirements">
 <title>Требования к памяти и дисковому пространству</title>

<para>У вас должно быть как минимум &minimum-memory; памяти и &minimum-fs-size; дискового пространства. Для минимальной консольной системы (все стандартные пакеты) требуется 250MB. Если заходите установить разумное количество программ, включая систему X Window и некоторые программы и библиотеки дял разработчика, вам понадобится как минимум 400MB. Для более-менее полной desktop-системы вам понадобится несколько гигабайт. </para><para arch="m68k">На системах Amiga размер FastRAM соответствует общим требованиям к объёму памяти. Использование карт Zorrot с 16-битной памятью не поддерживается, потребуется 32-битная память. Для отключения 16-битной памяти можно использовать команду <command>amiboot</command>, см. <ulink url="&url-m68k-faq;">FAQ по Linux/m68k</ulink>. Последние версии ядра отключают 16-битную память автоматически. </para><para arch="m68k">На системах Atari Linux использует как ST-RAM, так и Fast RAM (TT-RAM). Многие пользователи сообщали о проблемах при работе самого ядра в Fast RAM, поэтому программа установки для Atari размещает ядро в ST-RAM. Требуется, как минимум, 2 МБ ST-RAM. Понадобится также не менее 12 МБ TT-RAM. </para><para arch="m68k">На системах Macintosh следует уделить внимание машинам с видео в памяти (RBV). Сегмент оперативной памяти с физическим адресом 0 используется для хранения содержимого экрана, что не позволяет загрузить ядро туда, куда оно загружается по умолчанию. Сегмент памяти, используемый для ядра и диска памяти должен быть не менее 4 МБ. </para><para arch="m68k">

<emphasis condition="FIXME">FIXME: это всё ещё верно?</emphasis>

</para>

 </sect1>
