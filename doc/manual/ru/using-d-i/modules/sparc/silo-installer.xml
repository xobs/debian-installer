<!-- retain these comments for translator revision tracking -->
<!-- $Id: silo-installer.xml 16467 2004-05-30 08:11:23Z mck-guest $ -->

  <sect3 arch="sparc">
  <title>Установить системный загрузчик <command>SILO</command> на жёсткий диск</title>
<para>Стандартный системный загрузчик для &architecture; называется <quote>silo</quote>. Его документация лежит в каталоге <filename>/usr/share/doc/silo/</filename>. <command>SILO</command> по настройке и использованию, за некоторыми исключениями, похож на <command>LILO</command>. Прежде всего, <command>SILO</command> позволяет вам загружать любой образ ядра с диска, даже если его не в <filename>/etc/silo.conf</filename>. То есть <command>SILO</command> умеет работать с разделами Linux. Также, <filename>/etc/silo.conf</filename> читается в загрузки, поэтому не нужно перезапускать <command>silo</command> после установки нового ядра, в отличии от <command>LILO</command>. <command>SILO</command> может также читать с разделов UFS, то есть оо может загружать разделы SunOS/Solaris. Это полезно, если вы хотите установить GNU/Linux рядом с существующим SunOS/Solaris. </para>
  </sect3>
