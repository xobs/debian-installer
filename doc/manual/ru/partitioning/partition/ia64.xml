<!-- retain these comments for translator revision tracking -->
<!-- $Id: ia64.xml 18660 2004-07-28 04:49:59Z dannf $ -->


  <sect2 arch="ia64"><title>Разметка для &arch-title;</title>

<para>Программа для дисковой разметки <command>partman</command> является инструментом разметки по умолчанию для программы установки. Она управляет имеющимися разделами и их точками монтирования, чтобы проверить, что диски и файловые системы настроены правильно для успешной установки. На самом деле для разметки используется программа <command>parted</command>. </para>

   <note>
   <title>Распознаваемые EFI форматы</title>
<para>Микропрограмма EIF на IA64 поддерживает два формата таблицы разделов (или метки диска), GPT и MS-DOS. MS-DOS обычно используется на i386 PC, не рекомендуется для систем IA64. Хотя в программу установки включён<command>cfdisk</command>, вы должны использовать только <ulink url="parted.txt"> <command>parted</command></ulink>, так как только она может работать корректно и с таблицами GPT и с MS-DOS. </para></note>

<para>При автоматическом способе разметки <command>partman</command> создаётEFI раздел первым на диске. Также вы можете создать раздел с помощью<guimenuitem>Направляющая разметка</guimenuitem> из главного меню тем же способом, каким вы создавали раздел <emphasis>swap</emphasis>. </para><para>Программа разметки <command>partman</command> справится с большинством вариантов разметки. В тех редких случаев, когда необходимо вручную разметить диск, вы можете использоватьоболочку как описано выше и запустить утилиту <command>parted</command> прямо из командной строки. Если предположить, что вы хотите стереть весь диск и создатьGPT таблицу с несколькими разделами, то для этого нужно использовать примерно такую последовательность команд: </para><para>
<informalexample><screen>mklabel gpt
      mkpartfs primary fat 0 50
      mkpartfs primary linux-swap 51 1000
      mkpartfs primary ext2 1001 3000
      set 1 boot on
      print
      quit

</screen></informalexample>
</para><para>Таким образом создастся таблица разделов и три раздела будут использоваться в качествезагрузочного раздела EFI, раздела swap и корневой файловой системы. Наконец, установится флаг загрузкуи на раздел EFI. Разделы заданы в мегабайтах, в виде смещения начала и конца от начала диска. В примере мы создали файловую систему ext2 размером 1999МБ, начиная со смещения 1001МБ от начала диска. Заметим, что форматирование swap раздела с помощью <command>parted</command> может занять несколько минут, так как происходит сканирование плохих блоков. </para>
  </sect2>

  <sect2 arch="ia64"><title>Boot Loader Partition Requirements</title>

<para>

ELILO, the ia64 boot loader, requires a partition containing a FAT
file system with the <userinput>boot</userinput> flag set.
The partition must be big enough to hold the boot loader and any
kernels or RAMdisks you may wish to boot.  A minimum size would be
about 20MB, but if you expect to run with multiple kernels, then
128MB might be a better size.

</para><para>

The EFI Boot Manager and the EFI Shell fully support the GPT table
so the boot partition does not necessarily have to be the first
partition or even on the same disk.
This is convenient if you should forget to allocate the partition and
only find out after you have formatted the other partitions on your disk(s).
The <command>partman</command> partitioner checks for an EFI partition
at the same time it checks for a properly set up <emphasis>root</emphasis>
partition.
This gives you an opportunity to correct the disk layout before the
package install begins.
The easiest way to correct this omission is to shrink the last partition
of the disk to make enough free space for adding an EFI partition.

</para><para>

It is strongly recommended that you allocate the EFI boot partition
on the same disk as the <emphasis>root</emphasis> filesystem.

</para>

  </sect2>

  <sect2 arch="ia64"><title>EFI Diagnostic Partitions</title>

<para>

The EFI firmware is significantly more sophisticated than the usual
BIOS seen on most x86 PCs.
Some system vendors take advantage of the ability of the EFI to
access files and run programs from a hard disk filesystem to store diagnostics
and EFI based system management utilities on the hard disk.
This is a separate FAT format filesystem on the system disk.
Consult the system documentation and accessories that come with the
system for details.
The easiest time to set up a diagnostics partition is at the same time you
set up the EFI boot partition.

</para>

   </sect2>
