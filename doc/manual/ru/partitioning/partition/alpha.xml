<!-- retain these comments for translator revision tracking -->
<!-- $Id: alpha.xml 11648 2004-03-22 00:37:46Z joeyh $ -->


  <sect2 arch="alpha"><title>Разметка для &arch-title;</title>
<para>Если вы выбрали загрузкуиз SRM консоли, то вы должны использовать <command>fdisk</command> для разметки диска, так как это единственная программа, поддерживающая работу с BSD disk labels, который требуется для <command>aboot</command> (помните, загрузочный блок SRM несовместим с таблицей разделов MS-DOS - смотрите <xref linkend="alpha-firmware"/>). <command>debian-installer</command> запускает <command>fdisk</command> по умолчанию, если вы не загружаетесь с помощью <command>MILO</command>. </para><para>Если диск, который вы выбрали для разметки, уже содержит BSD disk label, то <command>fdisk</command> по умолчанию запускается в режиме BSD disk label. Иначе, вы должны воспользоваться командой `b', чтобы войти в режим disk label. </para><para>Если вы не хотите использовать диск с разметкой для Tru64 Unix или одного из свободных 4.4BSD-Lite клонов операционных систем (FreeBSD, OpenBSD, или NetBSD), советуем вам  <emphasis>не</emphasis> делать третий раздел, содержащий весь диск. Это не нужно для <command>aboot</command>, и фактически, может привести в замешательство, когда утилита <command>swriteboot</command>, используемая для установки <command>aboot</command> в загрузочный сектор, пожалуется на раздел, который перекрывается с загрузочным сектором. </para><para>Также, так как <command>aboot</command> записывается в несколько первых секторов диска (в данный момент, он занимает около 70 килобайт или 150 секторов), вы <emphasis>должны</emphasis> для него оставить достаточно пустого места в начале диска. Раньше, предлагалось сделать маленький раздел в начале диска, и оставить его неотформатированным. По причине, упомянутой выше, теперь мы предлагаем не делать этого на диске, который будет использоваться только из GNU/Linux. </para><para>Для установки на ARC, вы должны сделать маленький раздел FAT в начале диска для хранения <command>MILO</command> и <command>linload.exe</command> - 5 мегабайт должно хватить, смотрите <xref linkend="non-debian-partitioning"/>. К сожалению, создание файловой системы FAT из меню пока не работает, поэтому вы должны сделаьт это вручную из командной строки с помощью <command>mkdosfs</command> перед попыткой установить системный загрузчик. </para>
  </sect2>