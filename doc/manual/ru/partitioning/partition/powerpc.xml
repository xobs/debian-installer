<!-- retain these comments for translator revision tracking -->
<!-- $Id: powerpc.xml 23146 2004-10-16 14:20:10Z cjwatson $ -->


  <sect2 arch="powerpc"><title>Разметка новых PowerMac</title>
<para>Если вы производите установку на NewWorld PowerMac, то вы должны создать специальный загрузочный раздел для системного загрузчика. Размер этого раздела должен быть800КБ, а его тип <emphasis>Apple_Bootstrap</emphasis>. Если этот раздел создан без типа<emphasis>Apple_Bootstrap</emphasis>, то вашу машину нельзя будет загрузить с жёсткого диска. Этот раздел можно легко создать, создав новый раздел в программе <command>partman</command> и указав, что его нужно использовать в качестве<quote>загрузочного раздела NewWorld</quote>, или с помощью <command>mac-fdisk</command> используя команду <userinput>b</userinput>. </para><para>Специальный тип раздела Apple_Bootstrap требуется для предотвращения монтирования и повреждения загрузочного раздела MacOS, так как это изменение в OpenFirmware для автоматической загрузки. </para><para>Заметим, что этот загрузочный раздел нужен только для хранения 3 очень маленьких файлов: двоичный <command>yaboot</command>, его конфигурация <filename>yaboot.conf</filename> и первая часть загрузчика OpenFirmware <command>ofboot.b</command>. Его не нужно и он не должен быть смонтирован в файловую систему, на него не нужно копировать ядра и что-то другое. Для работы с этим разелом используются утилиты <command>ybin</command> и <command>mkofboot</command>. </para><para>Чтобы OpenFirmware автоматически загружал &debian; этот специальный загрузочный раздел должен располагаться перед любыми другими загрузочными разделами на диске, особенно перед загрузочным разделом MacOS. Специальный загрузочный раздел должен быть создан первым. Однако, если вы добавите специальный загрузочный раздел позднее, то можете воспользоваться  в программе <command>mac-fdisk</command> командной <userinput>r</userinput>, что преобразовать карту разделов для того чтобы специальный загрузочный раздел располагался сразу после карты (которая всегда является разделом 1). Это всего лишь логическое изменение, а не изменение физических адресов, что важно. </para><para>Диски Apple обычно содержат несколько маленьких разделов драйверов. Если вы хотите иметь двойную загрузку машины MacOSX, то нужно оставить эти разделы и маленьких раздел HFS (800к его минимальный размер). Это нужно для того чтобы MacOSX при каждой загрузке не предлагал инициализировать диски, которые не содержат активных разделов MacOS и разделов драйверов. </para>
  </sect2>
