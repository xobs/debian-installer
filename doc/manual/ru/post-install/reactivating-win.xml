<!-- retain these comments for translator revision tracking -->
<!-- $Id: reactivating-win.xml 23058 2004-10-12 23:51:17Z cjwatson $ -->


 <sect1 arch="i386" id="reactivating-win">
 <title>Реактивация DOS и Windows</title>
<para>После установки базовой системы и изменения<emphasis>главной загрузочной записи (MBR)</emphasis>, вы сможете загружать Linux, но вероятно ничего более. Это зависит от вашего выбора во время установки. Эта глава опишет, как вы можете реактивировать вашу старую систему, например, чтобы стала возможной загрузка DOS или Windows снова. </para><para><command>LILO</command> это менеджер загрузки, с помощью которого вы можететакже загружать другие операционные системы кроме Linux, которые подчиняются PC соглашениям. Менеджер загрузки настраивается через файл <filename>/etc/lilo.conf</filename>. После редактирования этого файла, вы должны выполнить программу <command>lilo</command>. Это нужно для того, чтобы ваши изменения вступили в силу. </para><para>Важными частями файла<filename>lilo.conf</filename> являются строки, содержащие ключевые слова <userinput>image</userinput> и <userinput>other</userinput>, а также строки, стоящие после них. Они используются для описания системы, которая может быть загружена с помощью <command>LILO</command>. Такая система может иметь ядро (<userinput>image</userinput>), корневой раздел, дополнительные параметры ядра и так далее, а также конфигурацию для загрузки другой, не Linux,  (<userinput>other</userinput>) операционной системы. Эти ключевые слова могут быть использованы несколько раз. Порядок этих систем в файле конфигурации важен, так как им определяется, какая система будет грузиться автоматически после, например, таймаута(<userinput>delay</userinput>) если <command>LILO</command> не остановленонажатием клавиши <keycap>shift</keycap>. </para><para>После установки Debian, только текущая система прописана в загрузку с помощью <command>LILO</command>. Если нужно загрузить другое ядро Linux, вы можете отредактировать файл конфигурации <filename>/etc/lilo.conf</filename>, добавив следующие строки: <informalexample><screen>

&additional-lilo-image;

</screen></informalexample> Для простой настройки достаточно только две первые строки. Если вы хотите больше узнать об этих двух параметрах, обраиттесь кдокументации на <command>LILO</command>. Её можно найти в каталоге<filename>/usr/share/doc/lilo/</filename>. Файл называется<filename>Manual.txt</filename>. Чтобы быстро войти в мир загрузки систем также посмотритестраницы руководства на <command>LILO</command> <filename>lilo.conf</filename>, где описаны ключевые слова файла конфигурации, а в странице <filename>lilo</filename> описана установка новой конфигурации в загрузочный сектор. </para><para>Обратите внимание, что в &debian; есть и другие системные загрузчики, такие как GRUB (в пакете <classname>grub</classname>), CHOS (в пакете <classname>chos</classname>), Extended-IPL (в пакете <classname>extipl</classname>), loadlin (в пакете <classname>loadlin</classname>) и другие. </para>
 </sect1>
