<!-- retain these comments for translator revision tracking -->
<!-- $Id: orientation.xml 16557 2004-06-01 03:04:31Z rudy-guest $ -->


 <sect1 id="debian-orientation"><title>Самостоятельное ориентирование в Debian</title>
<para>Debian слегка отличается от других дистрибутивов. Даже если вы знакомы с Linux по другим дистрибутивам, есть вещи которые вы должны знать о Debian, чтобы поддерживать систему в хорошем чистом состоянии. Эта глава содержит материалы, которые помогут вам ориентироваться; они не предназначены служить руководством по использованию Debian, а лишь бросают мимолётный взгляд на систему, для торопящихся людей. </para>

  <sect2><title>Система управления пакетами Debian</title>
<para>Наиболее важно понять систему управления пакетами Debian. По существу, большая часть системы будет находится под контролем системы управления пакетами. Система состоит: <itemizedlist> <listitem><para> <filename>/usr</filename> (кроме <filename>/usr/local</filename>) </para></listitem> <listitem><para> <filename>/var</filename> (вы можете сделать <filename>/var/local</filename> и это безопасно) </para></listitem> <listitem><para> <filename>/bin</filename> </para></listitem> <listitem><para> <filename>/sbin</filename> </para></listitem> <listitem><para> <filename>/lib</filename> </para></listitem> </itemizedlist> Например, если вы заместите <filename>/usr/bin/perl</filename>, он будет работать, но позже, если вы обновите пакет <classname>perl</classname>, файл, который вы положили будет заменён. Эксперты могут обойти это поставив пакету флаг ``hold'' в <command>aptitude</command>. </para><para>Один из лучших методов установки это apt. Вы можете пользоваться версией для командной строки <command>apt-get</command> или полноэкранной текстовой версией <application>aptitude</application>. Заметим, что apt также позволяет объединить main, contrib и non-free, так что вы можете установить несвободные пакеты вместе со стандартными версиями. </para>
  </sect2>

  <sect2><title>Управление версиями приложений</title>
<para>Альтернативность версий приложений управляется с помощью update-alternatives. Если вы работаете с несколькими версиями приложений, прочитайте страницу руководства к update-alternatives. </para>
  </sect2>

  <sect2><title>Управление заданиями cron</title>
<para>Любые работы в сфере системного администрирования должны распологаться в <filename>/etc</filename>, так как они являются конфигурационными файлами. Если у вас есть ежедневные, еженедельные или ночные задания cron для суперпользователя, поместите их в <filename>/etc/cron.{daily,weekly,monthly}</filename>. Они вызываются из <filename>/etc/crontab</filename>, и запускаются в алфавитном порядке. </para><para>С другой стороны, если задания (а) нужно запускать от определённого пользователя или (b) нужно запустить в определённое время или с определённой частотой, вы можете использовать или <filename>/etc/crontab</filename>, или, что лучше, <filename>/etc/cron.d/имя_задания</filename>. Эти особые файлы, которые помимо прочего содержат дополнительное поле, позволящее вам указать пользователя от которого будет выполняться задание cron. </para><para>В любом случае, просто отредактируйте файлы, и cron обнаружит эти изменения автоматически. Не нужно запускать специальную команду. Подробная информация доступна в cron(8), crontab(5) и <filename>/usr/share/doc/cron/README.Debian</filename>. </para>
  </sect2>
 </sect1>
