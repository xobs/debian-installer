<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- $Id$ -->
<!-- original version: 1.7 -->

<!--  This is not set off for i386 only, because many people will have -->
<!--  access to a PC in order to make a floppy for other arches. --> 

  <sect2><title>Gravando Imagens de Disquetes a partir do DOS, Windows ou OS/2</title>

<para condition="workaround-bug-99926">

Se tiver acesso a uma máquina i386, você poderá usar um 
dos seguintes programas para copiar as imagens para 
os disquetes.
<!-- links from 99926 info -->

</para><para>

O FDVOL, WrtDsk ou RaWrite3 podem ser usados sob o  MS-DOS.

</para><para condition="workaround-bug-99926">

<ulink url="http://www.minix-vmd.org/pub/Minix-vmd/dosutil/">
</ulink>

</para><para condition="workaround-bug-99926">

Para usar estes três programas, primeiro tenha certeza que
inicializou no DOS. Tentando usar estes programas dentro de uma
janela DOS dentro do Windows ou dando um clique duplo dentro
do Windows Explorer <emphasis>não</emphasis> é um método esperado
que eles funcionem. Se não sabe como inicializar diretamente no 
DOS, simplesmente pressione <keycap>F8</keycap> durante a 
inicialização.

</para><para>

O <command>NTRawrite</command> é um freeware contemporário da versão 
<command>Rawrite/Rawrite3</command> que é compatível com o WinNT e
Win2K. É uma aplicação gráfica auto-explicável; você seleciona a unidade
de disquetes que será gravada, navega para selecionar a imagem de disco
e pressiona o botão Write.

</para><para>

<ulink url="http://sourceforge.net/projects/ntrawrite/"></ulink>

</para>
  </sect2>

<!-- FIXME: É questionável como e quanto isso será suportado no d-i -->
  <sect2 id="set-lang">
   <title>
     Modificando o disquete de Recuperação para Suporta o Idioma nacional
   </title>

<para>

As mensagens mostradas pelo disquete de recuperação 
(antes de carregar o kernel do Linux) podem ser mostradas em sua 
lingua mãe. Para fazer isto se não for uma pessoa que fale o 
idioma inglês, após gravar o arquivo de imagem, você deverá 
copiar os arquivos de mensagem fornecidos e a fonte para o disqeute.
Para os usuários do MS-DOS e Windows, existe um programa chamado
<filename>setlang.bat</filename> no diretório <filename>dosutils</filename>.
Simplesmente entre neste diretório (e.g. <userinput>cd
c:\debian\dosutils
</userinput>) dentro da janela de comandos e execute
<userinput>setlang <replaceable>lang</replaceable></userinput>, 
onde <replaceable>lang</replaceable> é o código de 2 letras do
país em minúsculas, por exemplo
<userinput>setlang pl</userinput> para ajustar o idioma para Polonês. 
Atualmente estes códigos de idioma estão disponíveis: 
<informalexample><screen>
&setlang-language-code-list;
</screen></informalexample> 

</para><para>

<!-- TRANSLATORS: Please replace "non localized (English)" -->
<!-- in the following paragraph with "localized (your-language)" -->
<!-- Feito (Gleydson) -->

Note que as descrições deste manual assumem que você usa uma
instalação traduzida (pt_BR); caso contrário, os nomes dos 
menus e botões poderão ser diferentes dos mostrados em sua 
tela.
</para>

  </sect2>
