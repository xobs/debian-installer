<?xml version="1.0" encoding="EUC-JP"?>
<!-- retain these comments for translator revision tracking -->
<!-- $Id$ -->
<!-- original version: 11648 -->

   <sect2 id="partman"><title>ディスクのパーティション分割</title>
<para>

<!--
Now it is time to partition your disks. If you are uncomfortable with
partitioning, or just want to know more details, see <xref
linkend="partitioning"/>.
-->
さあ、ディスクのパーティションを分割しましょう。
パーティション分割に不安があったり、詳細を知りたければ、 
<xref linkend="partitioning"/>をご覧ください。

</para><para>

<!--
First you will be given the opportunity to automatically partition
either an entire drive, or free space on a drive. If you do not want
to autopartition, choose <guimenuitem>Manually edit partition
table</guimenuitem> from the menu and skip to the next
paragraph. Otherwise you will be asked if you want <guimenuitem>All
files in one partition</guimenuitem>, or if you rather like to have
a <guimenuitem>Separate partition for the home
directories</guimenuitem>. Both schemes have their pros and cons, some
of which are discussed in <xref linkend="partitioning"/>. If you are
unsure, choose the first one.
-->
最初に、自動的にドライブのすべての領域を分割するか、
またはドライブを自由に分割するかを、選択する機会が与えられます。
自動分割を望まなければ、
<guimenuitem>Manually edit partition table</guimenuitem> を選び、
次の段落にスキップしてください。
そうでなければ、<guimenuitem>All files in one partition</guimenuitem> や、
<guimenuitem>Separate partition for the home directories</guimenuitem>
と尋ねられるでしょう。
どちらの案でも賛否両論あり、<xref linkend="partitioning"/> で議論されています。
よくわからなければ、最初の項目を選択してください。

</para><para>

<!--
On the next screen you will see your partition table, how the
partitions will be formatted, and where they will be mounted.
If you did automatic partitioning, you should just be able to choose
<guimenuitem>Finished partitioning</guimenuitem> from the menu to use
what it set up.
-->
次の画面でパーティションテーブル (パーティションをどうフォーマットするか、
それをどこにマウントするか) を見ることになります。
もし自動パーティション分割を行っていれば、設定したものを使用するメニューから、
<guimenuitem>Finished partitioning</guimenuitem> で決定できます。

</para><para>

<!--
Select partititons from the list to modify or delete them. If you have
free space it will also show up under a drive, and you can select it
to create new partitions. When modifying a partition you will have the
opportunity to choose the file system to use, and where to mount
it. The partitioning menu also has a choice at the bottom that can be
used to automatically partition a drive or existing free space on
a drive, if you'd rather go that route. Be sure to create at least two
partitions, one for <emphasis>swap</emphasis> and one for the
<emphasis>root</emphasis> filesystem (which must be mounted as
<filename>/</filename>). If you forget to mount the root filesystem,
<command>partman</command> won't let you continue until you correct
this issue.
-->
修正・削除するパーティションをリストから選択してください。
ドライブに空き領域があれば、
新しいパーティションを作成するのに選択することができます。
パーティションを修正する場合、使用するファイルシステムやマウントする場所を、
選択することができます。
あえて行うなら、ドライブやドライブ上の空き領域を自動的に分割する選択肢が、
パーティション分割メニューの一番下にあります。
少なくとも 2 つのパーティションを必ず作成してください。
1 つは <emphasis>swap</emphasis> で、
もう 1 つは (<filename>/</filename> にマウントする) 
<emphasis>ルート</emphasis>ファイルシステムです。
ルートファイルシステムをマウントし忘れると、
この問題を修正するまで <command>partman</command> は先に進みません。

</para><para>

<!--
Capabilities of <command>partman</command> can be extended with
installer modules, so if you can't see all promised goodies, check if
you have loaded all required modules
(e.g. <filename>partman-ext3</filename>,
<filename>partman-xfs</filename>, or
<filename>partman-lvm</filename>).
-->
<command>partman</command> の能力は、インストーラモジュールで拡張できます。
あるはずの機能を確認できなければ、すべての必要なモジュールが読み込まれているか
確認してください。
(例: <filename>partman-ext3</filename>,
<filename>partman-xfs</filename>, 
<filename>partman-lvm</filename>)

</para><para>

<!--
After finishing partitioning, select <guimenuitem>Finished
partitioning</guimenuitem> from the menu, and confirm that the
filesystems should be created as requested.
-->
パーティション分割が完了したら、メニューから 
<guimenuitem>Finished partitioning</guimenuitem> を選択し、
指定したファイルシステムが作成されているか確認してください。

</para>
   </sect2>
