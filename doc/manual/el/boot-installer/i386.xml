<!-- retain these comments for translator revision tracking -->
<!-- $Id$ -->

  <sect2 arch="i386"><title>Booting from a CD-ROM</title>

&boot-installer-intro-cd.xml;

<!-- We'll comment the following section until we know exact layout -->
<!-- 
CD #1 of official Debian CD-ROM sets for &arch-title; will present a
<prompt>boot:</prompt> prompt on most hardware. Press
<keycap>F3</keycap> to see the list of kernel options available
from which to boot. Just type your chosen flavor name (idepci,
vanilla, compact, bf24) at the <prompt>boot:</prompt> prompt
followed by &enterkey;.

</para><para>

If your hardware doesn't support booting of multiple images, put one
of the other CDs in the drive. It appears that most SCSI CD-ROM drives 
do not support <command>isolinux</command> multiple image booting, so users
with SCSI CD-ROMs should try either CD2 (vanilla) or CD3 (compact), 
or CD5 (bf2.4). 

</para><para>

CD's 2 through 5 will each boot a 
different ``flavor'' depending on which CD-ROM is
inserted. See <xref linkend="kernel-choice"/> for a discussion of the
different flavors.  Here's how the flavors are laid out on the
different CD-ROMs:

<variablelist>
<varlistentry>
<term>CD 1</term><listitem><para>

Allows a selection of kernel images to boot from (the idepci flavor is
the default if no selection is made). 

</para></listitem></varlistentry>
<varlistentry>
<term>CD 2</term><listitem><para>

Boots the `vanilla' flavor.

</para></listitem></varlistentry>
<varlistentry>
<term>CD 3</term><listitem><para>

Boots the `compact' flavor.

</para></listitem></varlistentry>
<varlistentry>
<term>CD 4</term><listitem><para>

Boots the `idepci' flavor.

</para></listitem></varlistentry>
<varlistentry>
<term>CD 5</term><listitem><para>

Boots the `bf2.4' flavor.

</para></listitem></varlistentry>

 </variablelist>

</para><para>

-->

  </sect2>

<!-- FIXME the documented procedure does not exactly work, commented out
     until fixes

  <sect2 arch="i386" id="install-from-dos">
  <title>Booting from a DOS partition</title>

&boot-installer-intro-hd.xml;

<para>

Boot into DOS (not Windows) without any drivers being loaded.  To do
this, you have to press <keycap>F8</keycap> at exactly the right
moment (and optionally select the `safe mode command prompt only'
option).  Enter the subdirectory for the flavor you chose, e.g.,

<informalexample><screen>

cd c:\install

</screen></informalexample>.  

Next, execute <command>install.bat</command>.
The kernel will load and launch the installer system. 

</para><para>

Please note, there is currently a loadlin problem (#142421) which
precludes <filename>install.bat</filename> from being used with the
bf2.4 flavor. The symptom of the problem is an
<computeroutput>invalid compressed format</computeroutput> error.

</para>
  </sect2>

END FIXME -->

  <sect2 arch="i386" id="boot-initrd">
  <title>Ξεκινώντας από Linux με <command>LILO</command> ή
  <command>GRUB</command></title>

<para>
Για να ξεκινήσετε τον εγκαταστάτη από τον σκληρό δίσκο θα πρέπει πρώτα να
κατεβάσετε και να τοποθετήσετε τα απαραίτητα αρχεία όπως περιγράφεται στην ενότητα
<xref linkend="boot-drive-files"/>.
</para>

<para>
Αν σκοπεύετε να χρησιμοποιήσετε τον σκληρό δίσκο μόνο για την εκκίνηση και στη
συνέχεια να κατεβάσετε ο,τιδήποτε άλλο από το δίκτυο, θα πρέπει να κατεβάσετε το
αρχείο <filename>netboot/debian-installer/i386/initrd.gz</filename> και τον
αντίστοιχο πυρήνα. Αυτό θα σας επιτρέψει να επαναδιαμερίσετε το δίσκο από τον
οποίο θα εκκινήσετε τον εγκαταστάτη, αν και θα πρέπει να κάνετε κάτι τέτοιο με
προσοχή.
</para>

<para>
Εναλλακτικά, αν σκοπεύετε να κρατήσετε αμετάβλητη μια υπάρχουσα διαμέριση στον
σκληρό δίσκο κατά τη διάρκεια της εγκατάστασης, μπορείτε να κατεβάσετε 
το αρχείο <filename>hd-media/initrd.gz</filename>
και τον πυρήνα του, καθώς και να αντιγράψετε ένα CD iso (βεβαιωθείτε ότι το αρχείο
έχει κατάληξη σε ".iso"). Ο εγκαταστάτης μπορεί τότε να εκκινήσει από τη συσκευή
του CD-ROM και να κάνει την εγκατάσταση από την εικόνα του CD iso, χωρίς την ανάγκη
δικτύου.
</para>

<para>
Για το  <command>LILO</command>, θα πρέπει να ρυθμίσετε δυο ουσιαστικά στοιχεία
στο αρχείο <filename>/etc/lilo.conf</filename>:
<itemizedlist>
<listitem><para>

για να φορτώσει τον εγκαταστάτη <filename>initrd.gz</filename> κατά τη διάρκεια της
εκκίνησης;

</para></listitem>
<listitem><para>

να κάνετε τον πυρήνα  <filename>vmlinuz</filename> να χρησιμοποιήσει έναν δίσκο RAM
σαν τη ριζική του διαμέριση.

</para></listitem>
</itemizedlist>

Εδώ είναι ένα παράδειγμα αρχείου <filename>/etc/lilo.conf</filename>:

</para><para>

<informalexample><screen>

image=/boot/newinstall/vmlinuz
       label=newinstall
       initrd=/boot/newinstall/initrd.gz
       root=/dev/ram0
       append="devfs=mount,dall ramdisk_size=17000"

</screen></informalexample>

Για περισσότερες λεπτομέρειες, αναφερθείτε στις σελίδες χρήσης των
<citerefentry><refentrytitle>initrd</refentrytitle>
<manvolnum>4</manvolnum></citerefentry> και
<citerefentry><refentrytitle>lilo.conf</refentrytitle>
<manvolnum>5</manvolnum></citerefentry>. Τρέξτε τώρα το
<userinput>lilo</userinput> και επανεκκινήστε.

</para><para>

Η  διαδικασία για το  <command>GRUB</command> είναι αρκετά παρόμοια.
Εντοπίστε το το αρχείο <filename>menu.lst</filename> στον κατάλογο
<filename>/boot/grub/</filename> (μερικές φορές στον κατάλογο
<filename>/boot/boot/grub/</filename>),
και προσθέστε τις ακόλουθες γραμμές:

<informalexample><screen>

title  New Install
kernel (hd0,0)/boot/newinstall/vmlinuz root=/dev/ram0 devfs=mount,dall ramdisk_size=17000
initrd (hd0,0)/boot/newinstall/initrd.gz

</screen></informalexample>

και επανεκκινήστε. Σημειώστε ότι η τιμή
<userinput>ramdisksize</userinput>
πιθανόν να χρειάζεται προσαρμογή για το μέγεθος της εικόνας initrd. 
Από το σημείο αυτό και μετά, δεν θα υπάρχει διαφορά ανάμεσα
στα <command>GRUB</command> και <command>LILO</command>.

</para>
  </sect2>

  <sect2 arch="i386" id="usb-boot">
  <title>Εκκίνηση από USB stick μνήμης</title>
<para>

Ας υποθέσουμε ότι έχετε προετοιμάσει όσα υποδεικνύονται στις ενότητες  <xref
linkend="boot-dev-select"/> και <xref linkend="boot-usb-files"/>.  Τώρα απλά
συνδέστε το USB stick σε μια ελεύθερη θύρα σύνδεσης USB connector και
επανεκκινήστε τον υπολογιστή.  Το σύστημα θα πρέπει να ξεκινήσει, και θα εμφανιστεί
μπροστά σας το προτρεπτικό <prompt>boot:</prompt>.  Εδώ μπορείτε να εισάγετε
προαιρετικά ορίσματα για την εκκίνηση ή απλά να πατήσετε &enterkey;.

</para><para>

Σε περίπτωση που ο υπολογιστής σας δεν υποστηρίζει εκκίνηση από USB συσκευές μνήμης
μπορείτε ακόμα να χρησιμοποιήσετε μια μόνο δισκέττα για την αρχική εκκίνηση και
μετά να αλλάξετε σε USB. Εκκινήστε το σύστημά σας όπως περιγράφεται στην ενότητα
<xref linkend="floppy-boot"/>;
ο πυρήνας στη δισκέττα εκκίνησης θα πρέπει να ανιχνεύσει το USB
stick αυτόματα.
Όταν ρωτηθείτε για τη δισκέττα "root", πατήστε απλά &enterkey;. Θα πρέπει να δείτε
τον &d-i; να ξεκινά.

</para>
  </sect2>

  <sect2 arch="i386" id="floppy-boot">
  <title>Εκκίνηση από Δισκέττες </title>
<para>

Θα έχετε ήδη κατεβάσει τις εικόνες των δισκεττών που χρειάζεστε και δημιουργήσει
απο αυτές τις αντίστοιχες δισκέττες στο <xref
linkend="create-floppy"/>.
<!-- missing-doc FIXME If you need to, you can also modify the boot floppy; see 
<xref linkend="rescue-replace-kernel"/>. -->

</para><para>

Για να εκκινήσετε από τη δισκέττα εκκίνησης του εγκαταστάτη, τοποθετήστε τη
δισκέττα στην κύρια οδηγό δισκεττών, κλείστε το σύστημα κατά το συνηθισμένο τρόπο,
και μετά ξανανοίξτε το.

</para><para>

Για εγκατάσταση από έναν οδηγο LS-120 (έκδοση ATAPI)
με ένα σετ από δισκέττες, θα πρέπει να προσδιορίσετε την εικονική τοποθεσία της
συσκευής δισκέττας Αυτό γίνεται με το όρισμα <emphasis>root=</emphasis> στην
εκκίνηση, δίνοντας τη συσκευή στην οποία απεικονίζει ο οδηγός ide-floppy την αρχική
συσκευή. Για παράδειγμα, αν η συσκευή σας  LS-120 είναι συνδεδεμένη στην πρώτη
συσκευή IDE (κύρια) στο δεύτερο καλώδιο, θα πρέπει να βάλετε
<userinput>linux root=/dev/hdc</userinput> στο προτρεπτικό της εκκίνησης.
Εγκατάσταση από LS-120 υποστηρίζεται μόνο από πυρήνες  2.4 και νεώτερους.

</para><para>

Σημειώστε ότι σε μερικά μηχανήματα ο συνδυασμός πλήκτρων
<keycombo><keycap>Control</keycap>
<keycap>Alt</keycap> <keycap>Delete</keycap></keycombo>δεν επανεκκινεί το μηχάνημα
κανονικά, οπότε συνίσταται μια ``σκληρή'' επανεκκίνηση. Αν κάνετε την εγκατάσταση
από ένα υπάρχον λειτουργικό σύστημα (πχ., από ένα ``κουτί"  DOS) δεν έχετε
επιλογή. Διαφορετικά, παρακαλούμε να κάνετε μια "σκληρή" επανεκκίνηση κατά τη
διαδικασία της εκκίνησης.

</para><para>

Στη συνέχεια η δισκέττα θα είναι προσβάσιμη και θα δείτε μια οθόνη που εισάγει τη
δισκέττα και τελειώνει με το προτρεπτικό <prompt>boot:</prompt>.

</para><para>

Όταν πληκτρολογήσετε &enterkey;, θα δείτε το μήνυμα
<computeroutput>Loading...</computeroutput>, ακολουθούμενο από το
<computeroutput>Uncompressing Linux...</computeroutput>, και στη συνέχεια μια γεμάτη
οθόνη από πληροφορίες σχετικά με το υλικό του συστήματος. Περισσότερες πληροφορίες
γι' αυτή τη φάση της διαδικασίας μπορούν να βρεθούν στο  
<xref linkend="kernel-msgs"/>.

</para><para>

Μετά την εκκίνηση από τη δισκέττα εκκίνησης θα ζητηθεί η δισκέττα "root".
Εισάγετε τη δισκέττα αυτή και πατήστε  &enterkey;,
που το περιεχόμενό της φορτώνεται στην μνήμη. Το πρόγραμμα του εγκαταστάτη
<command>debian-installer</command> ξεκινά αυτόματα.

</para>
  </sect2>
  
  <sect2 arch="i386" id="boot-tftp"><title>Εκκίνηση με TFTP</title>
 
&boot-installer-intro-net.xml;
 
<para>

Υπάρχουν διάφοροι τρόποι για να κάνετε μια εκκίνηση με TFTP στην αρχιτεκτονική  i386.

</para>

   <sect3><title>Κάρτα δικτύου ή μητρική που υποστηρίζουν τη μέθοδο PXE</title>
<para>

Πιθανόν η κάρτα δικτύου σας (Network Interface Card) ή η μητρική σας να παρέχουν τη
δυνατότητα/λειτουργικότητα  για εκκίνηση με PXE.
Αυτή είναι μια επανα-υλοποίηση από την <trademark class="trade">Intel</trademark>
της εκκίνησης με  TFTP. Αν αυτό συμβαίνει πιθανόν να μπορείτε να ρυθμίσετε το BIOS
του συστήματος σας ώστε να εκκινεί από το δίκτυο.

</para>
   </sect3>
   
   <sect3><title>Κάρτα δικτύου (NIC) με δικτυακή bootROM</title>
<para>

Πιθανόν να υπάρχει η περίπτωση που η κάρτα δικτύου σας παρέχει τη δυνατότητα
εκκίνησης με TFTP.

</para><para condition="FIXME">

Ενημερώστε μας  (<email>&email-debian-boot-list;</email>) για τον τρόπο που τα
καταφέρατε. Παρακαλούμε αναφερθείτε στο κείμενο αυτό.

</para>
   </sect3>

   <sect3><title>Etherboot</title>
<para>

Tο <ulink url="http://www.etherboot.org">etherboot project</ulink>
διαθέτει δισκέττες εκκίνησης και ίσως ακόμα bootroms που να πραγματοποιούν μια
εκκίνηση με TFTPboot.

</para>
   </sect3>
  </sect2>

  <sect2 arch="i386"><title>Παράμετροι εκκίνησης για i386</title>
<para>

Όταν ξεκινά ο εγκαταστάτης, θα βρεθείτε μπροστά στο προτρεπτικό εκκίνησης,
<prompt>boot:</prompt> στο οποίο μπορείτε να κάνετε δυο πράγματα. Μπορείτε να
πατήσετε τα πλήκτρα συναρτήσεων από <keycap>F1</keycap> έως
<keycap>F10</keycap> για να δείτε μερικές σελίδες βοηθητικών πληροφοριών, ή να πατή
σετε <keycap>Enter</keycap> για την εκκίνηση του συστήματος.

</para><para>

Πληροφορίες σχετικά με τις παραμέτρους εκκίνησης που ίσως είναι χρήσιμες
μπορούν να βρεθούν πατώντας τα πλήκτρα  <keycap>F3</keycap> έως
<keycap>F7</keycap>.  Αν προσθέσετε οποιεσδήποτε παραμέτρους στην γραμμή εντολών
εκκίνησης, βεβαιωθείτε να πληκτρολογήσετε την μέθοδο εκκίνησης (η προκαθορισμένη
είναι η <userinput>linux</userinput>) και έναν κενό διάστημα πριν από την πρώτη
παράμετρο (πχ., <userinput>linux floppy=thinkpad</userinput>). Αν απλά
πατήσετε &enterkey;, αυτό είναι ισοδύναμο με το να
πληκτρολογήσετε <userinput>linux</userinput> χωρίς καμμία ειδική παράμετρο.

</para><para>

Μερικά συστήματα έχουν δισκέττες με ``αντεστραμμένα DCLs''. Αν έχετε σφάλματα στην
ανάγνωση της δισκέττας, ακόμα κι αν ξέρετε ότι η δισκέττα είναι καλή,
δοκιμάσετε την παράμετρο <userinput>floppy=thinkpad</userinput>.

</para><para>

Σε κάποια συστήματα, όπως τα IBM PS/1 ή ValuePoint
(που έχουν οδηγούς δίσκου  ST-506), η συσκευή IDE ίσως να μην αναγνωρίζεται
κανονικά. Και πάλι, δοκιμάστε στην αρχή χωρίς τις παραμέτρους και δείτε αν η
συσκευή IDE αναγνωρίζεται κανονικά. Αν όχι προσδιορίστε τη γεωμετρία του δίσκου
(κύλινδροι, κεφαλές και τομείς) και χρησιμοποιήστε τιην παράμετρο
<userinput>hd=<replaceable>cylinders</replaceable>,<replaceable>heads</replaceable>,
<replaceable>sectors</replaceable></userinput>.

</para><para>

Αν έχετε ένα πολύ παλιό μηχάνημα, και ο πυρήνας "κρεμάει" μετά το μήνυμα
<computeroutput>Checking 'hlt' instruction...</computeroutput>, τότε θα έπρεπε να
δοκιμάσετε το όρισμα  <userinput>no-hlt</userinput> στην εκκίνηση, που
απενεργοποιεί αυτό το τεστ.

</para><para>


Αν η οθόνη σας αρχίζει να εμφανίζει κάποια περίεργη εικόνα κατά την
εκκίνηση του πυρήνα, πχ. γίνεται εντελώς λευκή, εντελώς μαύρη ή δείχνει άσχετα
έγχρωμα pixel, είναι πιθανόν το σύστημά σας να έχει μια προβληματική κάρτα οθόνης
που δεν αλλάζει σωστά σε κατάσταση framebuffer.  
Μπορείτε τότε να χρησιμοποιήσετε  τις παραμέτρους εκκίνησης
<userinput>debian-installer/framebuffer=false</userinput> ή
<userinput>video=vga16:off</userinput> για να απενεργοποιήσετε την
κονσόλα framebuffer. Στην περίπτωση αυτή μόνο η αγγλική γλώσσα θα είναι διαθέσιμη
κατά την εγκατάσταση λόγω των περιορισμένων χαρακτηριστικών της κονσόλας. Δείτε το 
 <xref linkend="boot-parms"/> για λεπτομέρειες.


</para>
  </sect2>


  <sect2 arch="i386">
  <title>Πάγωμα του συστήματος κατά την φάση της ρύθμισης του  PCMCIA </title>

<para>.

Μερικά μοντέλα φορητών υπολογιστών που παράγονται από τη DELL είναι γνωστό ότι
παγώνουν μόλις η διαδικασία ανίχνευσης της συσκευής PCMCIA προσπαθεί να
προσπελάσει κάποιες διευθύνσεις υλικού. Αν αντιμετωπίσετε αυτό το πρόβλημα και δεν
χρειάζεστε την υποστήριξη PCMCIA κατά την εγκατάσταση, μπορείτε να απενεργοποιήσετε
την PCMCIA χρησιμοποιώντας την παράμετρο εκκίνησης
<userinput>hw-detect/start_pcmcia=false</userinput>. Μπορείτε τότε να ρυθμίσετε την
PCMCIA μετά την ολοκλήρωση της εγκατάστασης και να αποκλείσετε το φάσμα των πόρων
εκείνων που προκαλούν τα προβλήματα.

</para><para>

Εναλλακτικά μπορείτε να εκκινήσετε τον εγκαταστάτη στην κατάσταση
"έμπειρου χρήστη". Θα ρωτηθείτε τότε να εισάγετε τις επιλογές για την περιοχή των
πόρων που χρειάζεται το υλικό σας. Για παράδειγμα, αν έχετε έναν από τους φορητούς
υπόλογιστές Dell που προαναφέρθηκαν, θα πρέπει εδώ να εισάγετε την
επιλογή <userinput>exclude port 0x800-0x8ff</userinput>. Υπάρχει ακόμα και μια
λίστα μερικών κοινών επιλογών για την περιοχή των πόρων στή σελίδα <ulink
url="http://pcmcia-cs.sourceforge.net/ftp/doc/PCMCIA-HOWTO-2.html#ss2.5">System
resource settings section of the PCMCIA HOWTO</ulink>. Σημειώστε ότι θα πρέπει να
παραλείψετε τα κόμματα, αν υπάρχουν, όταν εισάγετε αυτή την τιμή στον εγκαταστάτη.


</para>
  </sect2>

  <sect2 arch="i386">
  <title>Πάγωμα του συστήματος κατά το φόρτωμα των USB modules</title>
<para>

Ο πυρήνας προσπαθεί κανονικά να εγκαταστήσει USB modules και τον οδηγό του
πληκτρολογίου USB ώστε να μπορέσει να υποστηρίξει μερικά όχι διαδεδομένα
πληκτρολόγια USB. Υπάρχουν όμως κάποια προβληματικά συστήματα USB στα οποία ο
οδηγός "παγώνει" κατά τη φόρτωση. Μια πιθανή λύση σε αυτό ίσως είναι η
απενεργοποίηση του ελεγκτή USB κατά τη ρύθμιση του BIOS της μητρικής. Μια άλλη
επιλογή είναι να δώσει κανείς την παράμετρο 
<userinput>debian-installer/probe/usb=false</userinput>στο προτρεπτικό της εκκίνησης,
που θα αποτρέψει τη φόρτωση των modules.

</para>
  </sect2>
