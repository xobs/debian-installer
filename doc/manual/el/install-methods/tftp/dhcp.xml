<!-- retain these comments for translator revision tracking -->
<!-- $Id: dhcp.xml 22664 2004-10-06 15:49:49Z fjpop-guest $ -->


  <sect2 condition="supports-dhcp" id="dhcpd">
   <title>Ρυθμίζοντας έναν DHCP server</title>
<para>

Ένας server DHCP που είναι ελεύθερο λογισμικό έιναι ο <command>dhcpd</command>. 
Στο &debian;, είναι διαθέσιμος στο πακέτο <classname>dhcp</classname>.
Αυτό είναι ένα παράδειγμα αρχείου ρυθμίσεων για τον server αυτόν (συνήθως
το αρχείο <filename>/etc/dhcpd.conf</filename>):

<informalexample><screen>

option domain-name "example.com";
option domain-name-servers ns1.example.com;
option subnet-mask 255.255.255.0;
default-lease-time 600;
max-lease-time 7200;
server-name "servername";

subnet 192.168.1.0 netmask 255.255.255.0 {
  range 192.168.1.200 192.168.1.253;
  option routers 192.168.1.1;
}

host clientname {
  filename "/tftpboot/tftpboot.img";
  server-name "servername";
  next-server servername;
  hardware ethernet 01:23:45:67:89:AB; 
  fixed-address 192.168.1.90;
}

</screen></informalexample>

Σημείωση: το καινούριο (και προτιμότερο) πακέτο
<classname>dhcp3</classname> χρησιμοποιεί το αρχείο
<filename>/etc/dhcp3/dhcpd.conf</filename>.

</para><para>

Στο παράδειγμα αυτό, υπάρχει ένας server με όνομα
<replaceable>"servername"</replaceable> που κάνει όλη τη δουλειά ενός
DHCP και TFTP server και μιας δικτυακής πύλης. Εϊναι σχεδόν βέβαιο ότι θα πρέπει
να αλλάξετε τις επιλογές για το όνομα τομέα (domain name) καθώς και το όνομα
του server και της διεύθυνσης της κάρτας δικτύου του πελάτη. Η επιλογή
<replaceable>"filename"</replaceable> θα πρέπει να είναι το όνομα του αρχείου
που θα ανακτηθεί μέσω TFTP.

</para><para>

Μετά την έκδοση του αρχείου ρυθμίσεων του
<command>dhcpd</command>,
επανεκκινήστε τον server με την εντολή <userinput>/etc/init.d/dhcpd
restart</userinput>.

</para>

   <sect3 arch="i386">
   <title>Ενεργοποίηση Εκκίνησης με τη μέθοδο PXE στη ρύθμιση του DHCP</title>
<para>
Εδώ έχουμε ένα άλλο παράδειγμα αρχείου <filename>dhcp.conf</filename> που
χρησιμοποιεί την μέθοδο Pre-boot Execution Environment (PXE) για το TFTP.

<informalexample><screen>

option domain-name "example.com";

default-lease-time 6048;
max-lease-time 604800;

allow booting;
allow bootp;

# The next paragraph needs to be modified to fit your case
subnet 192.168.1.0 netmask 255.255.255.0 {
  range 192.168.1.200 192.168.1.253;
  option subnet-mask 255.255.255.0;
  option broadcast-address 192.168.1.255;
# the gateway address which can be different 
# (access to the internet for instance)
  option routers 192.168.1.1;
# indicate the dns you want to use
  option domain-name-servers 192.168.1.3;
}

host tftpserver {
# tftp server ip address
  fixed-address 192.168.1.90;
# tftp server hardware address
  hardware ethernet 01:23:45:67:89:AB;
}

group {
 next-server 192.168.1.3;
 host tftpclient {
# tftp client hardware address
  hardware ethernet  00:10:DC:27:6C:15;
  filename "/tftpboot/pxelinux.0";
 }
}

</screen></informalexample>

Σημειώστε ότι για εκκίνηση με τη μέθοδο PXE, το όνομα αρχείου του
πελάτη <filename>pxelinux.0</filename>
είναι ένας φορτωτής εκκίνησης, και όχι μια εικόνα πυρήνα (δείτε την ενότητα <xref
linkend="tftp-images"/> παρακάτω).

</para>
   </sect3>
  </sect2>
