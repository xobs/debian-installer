<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 11648 -->

  <sect2 arch="mips" id="boot-tftp"><title>Amorcer avec TFTP</title>
<para>

Une fois entré dans le moniteur de commandes,
         utilisez

<informalexample><screen>

bootp(): root=/dev/ram0

</screen></informalexample>
sur les stations SGI Indy pour amorcer Linux et commencer
         l'installation de Debian. Pour ce faire, il sera peut-être
         nécessaire de déconfigurer la variable d'environnement
<envar>netaddr</envar>. Tapez

<informalexample><screen>

unsetenv netaddr

</screen></informalexample>
dans le moniteur de commandes pour le faire. 

</para>
  </sect2>


  <sect2 arch="mips"><title>Paramètres d'amorçage</title>
<para>

Sur les SGI Indy, vous pouvez ajouter des paramètres d'amorçage
      après la commande <command>bootp():</command> dans le moniteur de
      commandes.

</para><para>

Après la commande <command>bootp():</command>, vous pouvez ajouter le
      chemin et le nom du fichier à amorcer si vous ne donnez pas un
      nom explicite via votre serveur BOOTP/DHCP. Par exemple&nbsp;:
<command>bootp():/boot/tftpboot.img</command>.

</para>
  </sect2>
