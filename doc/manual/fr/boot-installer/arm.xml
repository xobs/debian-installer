<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 11648 -->

  <sect2 arch="arm" id="boot-tftp"><title>Amorcer à partir de TFTP</title>

&boot-installer-intro-net.xml;

  <sect3 arch="arm"><title>Amorcer à partir de TFTP sur une NetWinder</title>

<para>

Les NetWinders possèdent deux interfaces réseau&nbsp;:
        la carte 10&nbsp;Mb/s compatible NE2000 est l'interface réseau
        <filename>eth0</filename> et la carte Tulip en 100&nbsp;Mb/s est
        l'interface réseau <filename>eth1</filename>.

</para><note><para>

Vous aurez besoin de NeTTrom&nbsp;2.2.1 ou supérieur pour
      amorcer le système d'installation. Il est recommandé d'utiliser
      NeTTrom&nbsp;2.3.3&nbsp;: Vous pouvez l'obtenir à partir de
<ulink url="ftp://ftp.netwinder.org/pub/netwinder/firmware/"></ulink>:

<itemizedlist>
<listitem><para>

<filename>nettrom-2.3-3.armv4l.rpm</filename>

</para></listitem>
<listitem><para>

<filename>nettrom-2.3.3.bin</filename>

</para></listitem>
<listitem><para>

<filename>nettrom-2.3.3.bin.md5sum</filename>

</para></listitem>
</itemizedlist>

</para></note><para>

Après avoir réamorcé et interrompu le processus d'amorçage
        pendant la phase de compte à rebours, vous devez configurer le
        réseau avec l'adresse statique&nbsp;:
<informalexample><screen>

    NeTTrom command-> setenv eth0_ip 192.168.0.10/24

</screen></informalexample>
où 24 est le nombre de bits du masque réseau ou bien avec une
        adresse dynamique&nbsp;:
<informalexample><screen>

    NeTTrom command-> boot diskless

</screen></informalexample>

</para><para>

Il se peut que vous ayez aussi à configurer les réglages de
<userinput>route1</userinput> si le serveur TFTP n'est pas sur le
        sous-réseau local. Le reste de la configuration est tout à
        fait standard (l'étape de sauvegarde de l'ensemble est
        optionnel)&nbsp;:

<informalexample><screen>

    NeTTrom command-> setenv kerntftpserver 192.168.0.1
    NeTTrom command-> setenv kerntftpfile tftpboot.img
    NeTTrom command-> save-all
    NeTTrom command-> setenv netconfig_eth0 flash
    NeTTrom command-> setenv kernconfig tftp

</screen></informalexample>

</para><para>

Seules les deux dernières commandes interfèrent avec
        l'amorçage normal des disques donc il est prudent de tout
<command>save-all</command> sauvegarder soigneusement avant de le faire, ce qui permettra
        de sauvegarder les réglages réseau au cas où vous auriez à
        réamorcer depuis le réseau. Utilisez la commande
<command>printenv</command> pour réafficher vos réglages
        d'environnement. Finalement, si la variable de NeTTrom
<envar>cmdappend</envar> a l'option <option>noinitrd</option> (qui
        est nécessaire à l'amorçage des noyaux 2.4), vous devrez la
        supprimer pour que le noyau puisse amorcer avec ses disques
        virtuelles en attache.

</para>
  </sect3>

  <sect3 arch="arm"><title>Amorcer à partir de TFTP sur CATS</title>

<para>

Sur les machines CATS, utilisez <command>boot de0:</command>
        ou quelque chose de similaire à l'invite de Cyclone.

</para>
   </sect3>
  </sect2>



  <sect2 arch="arm"><title>Amorcer à partir d'un cédérom</title>

&boot-installer-intro-cd.xml;

<para>
Pour amorcer à partir d'un cédérom depuis l'invite sur une console
Cyclone, utilisez la commande
<command>boot cd0:cats.bin</command>
</para>
  </sect2>
