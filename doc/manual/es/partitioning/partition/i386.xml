<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 11648 -->

  <sect2 arch="i386"><title>Particionado en &arch-title;</title>
<para>

Generalmente, la BIOS del computador añade restricciones adicionales
para particionar el disco. Hay un límite al número de particiones
<quote>primarias</quote> y <quote>lógicas</quote> que se pueden crear. 
Adicionalmente, con las BIOS anteriores a 1994-98, existen limitaciones
sobre los dispositivos desde los cuales se puede iniciar el sistema.
Para mayor información, vea la documentación acerca de 
 <ulink url="&url-partition-howto;">cómo particionar en Linux</ulink> y la 
<ulink url="&url-phoenix-bios-faq-large-disk;">FAQ de la BIOS Phoenix</ulink>, 
pero de todas maneras, esta sección incluye una visión general para guiarlo
en la mayoría de las situaciones posibles.

</para><para>

Las particiones <quote>primarias</quote> son el método original
de particionado para discos. Sin embargo, solamente pueden haber cuatro de ellas. 
Para rebasar esta limitación, se inventaron las particiones de tipos
<quote>extendidas</quote> y <quote>lógicas</quote>. Estableciendo su
partición primaria como partición extendida, puede subdividir todo su
espacio asignado dentro de particiones lógicas. Puede crear hasta
60 particiones lógicas en cada partición extendida; sin embargo,
puede tener solamente una partición extendida por disco.

</para><para>

Linux limita a 15 particiones como máximo por cada disco SCSI
(3 particiones primarias, 12 particiones lógicas), y 63
particiones en una unidad IDE (3 particiones primarias, 60 particiones lógicas). 
Sin embargo, los sistemas normales de &debian; proveen sólo 20 dispositivos
por partición, por lo tanto, no podrá instalar sobre la partición número 20 o siguientes, 
a menos que haya creado manualmente los dispositivos para esas particiones.


</para><para>

Si tiene un disco IDE relativamente grande, y no esta utilizando LBA,
ni conductores de overlay (proporcionados a veces por los fabricantes
de los discos duros), la partición de inicio de su máquina (aquella que
contiene la imagen del núcleo de Linux) debe estar ubicada en los primeros
1024 cilindros de su disco duro (generalmente alrededor de 524 megabytes,
sin considerar la traducción que realiza la BIOS).
</para><para>

Esta restricción no se aplica si tiene una BIOS relativamente nueva,
(posterior a 1995-98, dependiendo del fabricante) que soporte las
<quote>Especificaciones avanzadas de los discos duros</quote>. 
Ambos Lilo, el cargador de Linux, y la alternativa de Debian 
<command>mbr</command> deben usar la BIOS para leer el núcleo desde el disco 
y cargarlo en la RAM. Si la BIOS dispone de las extensiones int 0x13 de acceso a
discos grandes, se usarán esas extensiones. De otra manera, se 
utilizará la interfaz primitiva de acceso al disco, que no se podrá usar
para acceder a posiciones superiores al cilindro 1023. Una vez que se inicie Linux,
no tiene importancia la BIOS que tenga, ya que las restricciones
ya no se aplican más, puesto a que Linux no utiliza la BIOS para acceder
a los discos.

</para><para>

Si posee un disco grande, podría tener que usar las técnicas para la traducción de
cilindros, las cuales pueden ser configuradas desde la BIOS, opciones, tales
como LBA (Dirección lógica de bloques) o traducción de modo (<quote>grande</quote>),
vía CHS.
Más información acerca de estos problemas con discos grandes se puede ver en
la <ulink url="&url-large-disk-howto;">Documentación acerca de discos grandes</ulink>. 
Si está usando un esquema de traducción de cilindros, y su BIOS no tiene la extensión
para soportar discos grandes, su partición de inicio debe estar dentro de los primeros
1024 cilindros (según la visión <emphasis>traducida</emphasis> de la la BIOS).

</para><para>

La manera recomendada de solucionar este problema, es creando una pequeña partición
(5 a 10MB deberían ser suficientes) al principio del disco para usadr como partición
de inicio, y después, en el área restante, crear las otras particiones que desee tener.
Esta partición de inicio, se <emphasis>debe</emphasis> montar en <filename>/boot</filename>,
ya que es en este directorio donde se almacenarán los núcleos de Linux.
Está configuración funcionará en cualquier sistema, sin importar si se utiliza
traducción LBA o CHS, ni tampoco si su BIOS soporta las extensiones para discos grandes.

</para>
  </sect2>
