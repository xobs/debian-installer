<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 11648 -->

 <sect1>
 <title>Esquemas recomendados para el particionamiento</title>
<para>

Para maquinas de usuarios novatos, equipos personales con Debian, sistemas para el hogar
y en general, cuando será usado por un solo usuario, una partición en <filename>/</filename>
(más swap), es probablemente la forma mas fácil de comenzar. Sin embargo, si su
partición es mas grande que 6GB, escoja ext3 como su tipo de partición. Las
particiones de tipo ext2, necesitan una revisión periódica de la integridad
del sistema de archivos, y esto puede tomar bastante tiempo al iniciar su sistema si
su partición demasiado grande.

</para><para>

Para sistemas multiusuario, o bien, con una gran cantidad de espacio
libre para usar, lo mejor es dejar <filename>/usr</filename>, <filename>/var</filename>,
<filename>/tmp</filename>, y <filename>/home</filename> separadadas de la
partición <filename>/</filename>.

</para><para>

Usted necesitará una partición <filename>/usr/local</filename> separada
si planea instalar una gran cantidad de programas que no son
parte del sistema Debian. Si su máquina va a ser un servidor de correo,
usted necesitará crear una partición separada para <filename>/var/mail</filename>.
A menudo, dejar una partición separada para <filename>/tmp</filename> 
es una muy buena idea. Es suficiente dejarla con 20 ó 50 MB.
Si está configurando un servidor con una gran cantidad de cuentas de usuarios,
es altamente recomendable dejar separada la partición <filename>/home</filename>.
En general, cómo se debe particionar el disco depende del uso que se le desee
dar al computador.


</para><para>

Para sistemas muy complejos, usted debiese leer el
<ulink url="&url-multidisk-howto;">
Multi Disk HOWTO</ulink>. Éste contiene información bastante profundizada, 
sobre todo del interés de los ISPs, y de la gente que desea instalar un servidor.

</para><para>

Con respecto al tamaño de la partición de intercambio (swap), hay una gran
cantidad de puntos de vista. Uno de ellos, que normalmente funciona muy bien, 
es asignar tanta swap como memoria tenga. En la mayoría de los casos no es 
recomendable que sea más pequeña que 16MB, en la mayoría de los casos. Obviamente, 
hay excepciones a estas reglas. Si usted está tratando resolver 10000 ecuaciones 
simultáneas en una mÁquina con 256 MB de memoria, necesitará un gigabyte (o más) 
de swap.

</para><para arch="m68k"> 

Por otro lado, los Ataris y los Macs no sufren muchos cambios dejando una
partición swap muy grande. Por lo tanto, en vez de crear una partición muy grande
para la swap, mejor obtenga la mayor cantidad de memoria RAM que sea posible.

</para><para>

En arquitecturas de 32 bits (i386, m68k, 32-bit SPARC, y PowerPC), 
el máximo para una partición de intercambio (swap) es de 2GB. Esto
debería ser suficiente para cualquier tipo de instalación. Sin embargo,
si sus requerisitos de swap son realmente altos, probablemente
debería tratar de separar la carga del intercambio, a través de diversos discos
(también llamados <quote>spindles</quote>) y, si es posible, en diferentes
canales SCSI o IDE. El núcleo va a balancear el uso de la swap entre múltiples
particiones de intercambio, dando así, un mejor rendimiento.

</para><para>

Como ejemplo, considere una máquina antigua que posee 32MB de RAM y un disco duro
de 1.7GB IDE en <filename>/dev/hda</filename>. 
En este caso sería conveniente dejar una partición de 500MB para otro sistema operativo en
<filename>/dev/hda1</filename>, 32MB para la partición de intercambio (swap) en 
<filename>/dev/hda3</filename> y alrededor de 1.2GB en
<filename>/dev/hda2</filename>) usada como la partición de Linux.

</para><para>

Para más ejemplos, revise las 
<ulink url="&url-partition-examples;">
estrategias para particionar</ulink>.

Para tener una idea de cuánto ocupa cada tarea que instale, puede
que le interese agregarlas después de completar su instalación.
Por favor revise <xref linkend="tasksel-size-list"/>
para mayor información.

</para>

 </sect1>
