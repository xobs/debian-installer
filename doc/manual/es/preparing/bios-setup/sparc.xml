<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- retain these comments for translator revision tracking -->
<!-- original version: 11648 -->


  <sect2 arch="sparc" id="invoking-openboot"><title>Invocar OpenBoot</title>

<para>

OpenBoot provee las funciones básicas que se necesitan para arrancar
la arquitectura &arch-title;. Esto es bastante similar en funciones
al BIOS de la arquitectura x86, aunque mucho mas simpático. Los PROMs
de arranque de Sun tiene un intérprete de embebido el cual en adelante
le permite hacer cierto número de cosas con su máquina, como diagnósticos,
programas simples, etc.

</para><para>

Para obtener el cursor de arranque debe mantener presionada la 
tecla <keycap>Stop</keycap> (en teclados antiguos de tipo 4, use
la tecla <keycap>L1</keycap>, si tiene un adaptador de teclado de
PC, use la tecla <keycap>Break</keycap>) y presiona la tecla
<keycap>A</keycap>. El PROM de arranque le mostrará un cursor, ya
sea <userinput>ok</userinput> o <userinput>&gt;</userinput>. Es
preferido tener el cursor <userinput>ok</userinput>. De modo que 
si obtiene el cursor del estilo antiguo, presione la tecla
<keycap>n</keycap> para obtener el cursor de nuevo estilo.

</para>
  </sect2>

  <sect2 arch="sparc" id="boot-dev-select-sun">
  <title>Selección de dispositivo de arranque</title>

<para>

Puede usar OpenBoot para arrancar desde dispositivos específicos, 
y también para cambiar el dispositivo de arranque predeterminado.
Sin embargo, necesita saber algunos detalles sobre como OpenBoot
nombra los dispositivos, es muy diferente del sistema de nombrado
de dispositivos de Linux descrito en <xref linkend="device-names"/>.
También, la orden varia un poco, dependiendo de que versión de
OpenBoot tenga. Puede encontrar mas información sobre OpenBoot en
la <ulink url="&url-openboot;">Referencia de OpenBoot de Sun</ulink>.

</para><para>

Típicamente, con últimas versiones puede usar un dispositivo de
OpenBoot como "floppy", "cdrom", "net", "disk" o "disk2". Estos
tienen el significado obvio, el dispositivo "net" es para arrancar
desde la red. Adicionalmente, el nombre de dispositivo puede
especificar una partición particular de un disco, como "disk2:a"
para arrancar la primera partición de disk2. Los nombres completos
de los dispositivos de OpenBoot tienen la forma:

<informalexample>
<screen>
<replaceable>nomre-controlador</replaceable>@
<replaceable>dirección-unidad</replaceable>:
<replaceable>argumentos-dispositivo</replaceable>
</screen></informalexample>.

En versiones antiguas de OpenBoot, el nombrado de dispositivos es un
poco diferente: El dispositivo de disquete es llamado "/fd" y los
dispositivos de disco SCSI tienen la forma: "sd(
<replaceable>controlador</replaceable>,
<replaceable>disk-target-id</replaceable>,
<replaceable>disk-lun</replaceable>)". El comando
<userinput>show-devs</userinput> en las últimas revisiones de
OpenBoot es útil para ver los dispositivos configurados actualmente.
Para información completa, cualquiera sea su revisión, vía la
<ulink url="&url-openboot;">Referencia de OpenBoot de Sun</ulink>.

</para><para>

Para arrancar desde un dispositivo específico, use la orden
<userinput>boot <replaceable>device</replaceable></userinput>.  
Puede definir este procedimiento como el predeterminado usando
la orden <userinput>setenv</userinput>. Sin embargo, el nombre
de la variable a definir cambia entre las revisiones de OpenBoot.
En OpenBoot 1.x, use la orden 
<userinput>setenv boot-from
<replaceable>device</replaceable></userinput>.  En revisiones
posteriores de OpenBoot, use la orden <userinput>setenv boot-device
<replaceable>device</replaceable></userinput>. Note que esto
también es configurable usando la orden <command>eeprom</command> 
en Solaris, o modificando los ficheros apropiados en
<filename>/proc/openprom/options/</filename>, por ejemplo desde Linux:

<informalexample><screen>

echo disk1:1 &gt;/proc/openprom/options/boot-device

</screen></informalexample> 

y desde Solaris:

<informalexample><screen>

eeprom boot-device=disk1:1

</screen></informalexample> 

</para>
  </sect2>
