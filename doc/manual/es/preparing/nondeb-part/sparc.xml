<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- retain these comments for translator revision tracking -->
<!-- original version: 11648 -->


  <sect2 arch="sparc"><title>Particionar desde SunOS</title>

<para>

Es perfectamente correcto particionar desde SunOS, de hecho, si
pretende ejecutar tanto SunOS como Debian en la misma máquina,
se recomienda que particione usando SunOS antes de la instalación
de Debian. El núcleo Linux entiende las etiquetas de disco de Sun,
así que no hay problemas allí. Solamente haga espacio para la 
partición raíz de Debian entre el área anterior a 1GB del disco
de arranque. También puede ubicar la imagen del núcleo en una
partición UFS si es mas simple que ubicar la partición raíz
allí. SILO soporta arrancar Linux y SunOS tanto desde 
particiones EXT2 (Linux), UFS (SunOS), romfs e iso9660 (CDROM).

</para>
  </sect2>

  <sect2 arch="sparc"><title>Particionar desde Linux u otro SO</title>

<para>

Cualquiera sea el sistema que use para particionar, asegúrese de crear
una "Etiqueta de disco Sun" (Sun disk label) en el disco de arranque.
Esta es la única forma de esquema de partición que entiende el PROM 
OpenBoot, y es el único esquema desde el que puede arrancar. En
<command>fdisk</command> la tecla <keycap>k</keycap> se usa para
crear etiquetas de disco Sun. Solamente debe hacer esto si todavía
no tiene una. Si esta usando un disco que ha sido previamente
formateado usando una PC (u otra arquitectura) debe crear una nueva
etiqueta de disco en caso contrario es bastante probable que 
ocurran problemas con la geometría del disco.

</para><para>

Probablemente usará <command>SILO</command> como gestor de arranque
(el pequeño programa que ejecuta el núcleo del sistema operativo).
<command>SILO</command> tiene ciertos requisitos para el tamaño y
ubicación de las particiones, vea <xref linkend="partitioning"/>.

</para>
  </sect2>
