<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- retain these comments for translator revision tracking -->
<!-- original version: 11648 -->


  <sect2 arch="i386"><title>Particionar desde DOS o Windows</title>
<para>

Si usted esta manipulando ficheros particiones FAT o NTFS existentes,
es recomendable que o use el esquema mostrado a continuación o 
herramientas nativas de Windows o DOS. De otro modo, realmente no es
necesario particionar desde DOS o Windows, las herramientas de particionado
de Linux generalmente harán un mejor trabajo.

</para><para>

Pero si tiene un disco IDE grande y esta usando ya sea direccionamiento
LBA, controladores sobrepuestos (aveces provistos por fabricantes de
discos duros) o un BIOS nuevo (fabricado después de 1998) que
soporta acceso a extensiones de discos grandes, entonces debe ubicar su
la partición de arranque de su sistema Debian cuidadosamente. En
este caso tendrá que ubicar la partición de arranque dentro de los
primeros 1024 cilindros de su disco duro (usualmente cerca de los 524 
megabytes, sin traducción de BIOS). Esto puede requerir que tenga que
mover una partición FAT o NTFS existente.

</para>

   <sect3 id="lossless">
   <title>Reparticionado sin pérdida cuando se inicia desde DOS, Win-32 u OS/2
   </title>

<para>

Una de las instalaciones mas comunes es en un sistema que ya tiene DOS
(incluyendo Windows 3.1), Win32 (como Windows 95, 98, Me,
NT, 2000, XP) o OS/2 y desea instalar Debian en el mismo disco sin
destruir el sistema anterior. Como se explica en 
<xref linkend="partition-sizing"/>, el reducir el tamaño de una 
partición existente es bastante probable que dañe la información en esta
partición a menos que se tomen ciertas precauciones. El método aquí
descrito, aunque no garantiza proteger su información, funciona
extremadamente bien en la práctica. Como precaución debe 
<emphasis>hacer copias de seguridad</emphasis>.

</para><para>

Antes de proseguir, deberá haber decidido como será la división del
disco. El método en esta sección solamente dividirá la partición en
dos partes. Una contendrá el sistema operativo original y la otra
será usado para Debian. Durante la instalación de Debian, se le
dará la oportunidad de usar la parte del disco perteneciente a
Debian como le parezca mejor, esto es como partición de intercambio
o como sistema de ficheros.

</para><para>

La idea es mover toda la información de la partición al inicio,
antes de cambiar la información de la partición, de modo que nada
se perderá. Es importante que intente en lo posible evitar el
movimiento de información y reparticionar para minimizar las
posibilidades de que se escriba un fichero cerca del final de la 
partición puesto que esto reducirá la cantidad de espacio de la
partición que usted puede utilizar.

</para><para>

Lo primero que se requiere es una copia de <command>fips</command> la
que esta disponible en el directorio <filename>tools/</filename> de
del mas cercano servidor espejo de Debian. Desempaquete el archivo y
copie los ficheros <filename>RESTORRB.EXE</filename>, 
<filename>FIPS.EXE</filename> y <filename>ERRORS.TXT</filename> a un
disquete arrancable. Puede crear un disquete arrancable usando la
orden <filename>sys a:</filename> en DOS. <command>fips</command> 
viene con muy buena documentación que usted debería leer. Definitivamente
necesitará leer la documentación si usted usa compresión de disco o un
gestor de disco. Cree el disco y lea la documentación 
<emphasis>antes</emphasis> que de-fragmente el disco.

</para><para>

Lo siguiente que necesitará es mover todos los datos al inicio de la
partición. <command>defrag</command>, que viene con el DOS 6.0 después
puede fácilmente realizar el trabajo. Vea la documentación de 
<command>fips</command> para una lista de otro software que pueda ayudarlo.
Note que si tiene Windows 9x, debe ejecutar <command>defrag</command> 
desde allí, puesto que DOS no reconoce el sistema de ficheros VFAT, el
cual es usado para soporte de ficheros de nombres largos, éste es usado
en Windows 95 y superior.

</para><para>

Después de ejecutar el defragmentador (lo que puede tomar un cierto
tiempo en un disco grande), reinicie con el disquete que creó con
el programa <command>fips</command> en él. Simplemente escriba
<filename>a:\fips</filename> y siga las instrucciones.

</para><para>

Note que existen muchos otros gestores de particiones, en caso de que
<command>fips</command> no pueda ayudarlo.

</para>
   </sect3>

   <sect3 id="partitioning-for-dos"><title>Particionar para DOS</title>

<para>

Si usted esta particionando para discos DOS o modificando el tamaño de
las particiones DOS usando herramientas de Linux, muchas personas
experimentan problemas al trabajar con las particiones DOS resultantes.
De hecho, algunos han informado mejor desempeño, problemas consistentes
con <command>scandisk</command> u otros fallos extraños en DOS o Windows.

</para><para>

Aparentemente, cada vez que cree o modifique una partición para
uso de DOS es una buena idea llenar los primeros sectores con ceros.
Haga esto antes de ejecutar el comando <command>format</command> de
DOS, puede ejecutar lo siguiente desde Linux:
<informalexample><screen>

dd if=/dev/zero of=/dev/hdXX bs=512 count=4

</screen></informalexample>

</para>
   </sect3>
  </sect2>
