<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- retain these comments for translator revision tracking -->
<!-- original version: 11648 -->


  <sect2 arch="powerpc"><title>Particionar en MacOS/OSX</title>

<para>

La aplicación <application>Apple Drive Setup</application> se puede
encontrar en el directorio <filename>Utilities</filename> del CD de MacOS.
éste programa no ajustará las particiones existentes, esta limitado a
particionar el disco entero por vez. Las particiones del controlador del
disco no son mostradas en <application>Drive Setup</application>.

</para><para>

Recuerde crear una partición provisional para GNU/Linux, preferiblemente
ubicada primero en la estructura del disco. No interesa de que tipo sea,
será borrada y reemplazada después en el instalador de &debian;.

</para><para>

Si planea instalar tanto MacOS 9 y OS X, es mejor crear particiones
separadas para OS 9 y OS X. Si éstos están instalados en la misma
partición se debe usar <application>Startup Disk</application> (y reiniciar)
para elegir entre ambos; la elección entre ambos sistemas no puede
hacerse al momento de arranque. Con particiones separadas, aparecerán
opciones separadas para OS 9 y OS X al mantener presionada la tecla 
<keycap>option</keycap> al momento del arranque, del mismo modo también
se puede instalar opciones separadas en el menú de arranque de 
<application>yaboot</application>. Además, Statup Disk desconocerá todas
las otras particiones, lo que puede afectar el arranque de GNU/Linux.  
Las particiones de OS 9 y OS X serán accesibles tanto desde OS 9 o OS X.

</para><para>

GNU/Linux no es capaz de acceder a la información en HFS+ (también
conocida como MacOS Extendida) o particiones UFS. OS X requiere uno de
estos dos tipos para su partición de arranque. MacOS 9 puede ser
instalado tanto en HFS (también conocida como MacOS Estándar) o
HFS+. Para compartir información entre sistemas MacOS y GNU/Linux
una partición para intercambio es útil. Las particiones HFS y MS-DOS
FAT son soportadas tanto por MacOS como por Linux.

</para>
  </sect2>
