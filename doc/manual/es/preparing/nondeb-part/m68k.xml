<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- retain these comments for translator revision tracking -->
<!-- original version: 11648 -->


  <sect2 arch="m68k"><title>Particionar en AmigaOS</title>
<para>

Si usted esta ejecutando AmigaOS, puede usar el programa
<command>HDToolBox</command> para ajustar sus particiones nativas
antes de la instalación.

</para>
  </sect2>

  <sect2 arch="m68k"><title>Particionar en Atari TOS</title>
<para>

Los IDs de particiones Atari tienen tres caracteres, use "LNX" para
datos y "SWP" para particiones de intercambio (swap). Si usted usa el
método de instalación para máquinas con poca memoria, se necesita
también una partición Minux (cerca de 2MB), para el cual el ID es "MNX".
Los fallos al definir el ID apropiado para la partición no solo impiden
el proceso de reconocimiento de las particiones en la instalación de Debian,
sino también puede ocasionar que TOS intente usar las particiones de
Linux, lo cual confunde al controlador del disco duro y convierte todo
el disco en inaccesible.

</para><para>

Existe un multitud de herramientas de particionado provista por terceros
disponible (la herramienta <command>harddisk</command> de Atari no
permite cambiar los ID de las particiones); este manual no puede dar
una descripción detallada para todos estos. La siguiente descripción
cubre <command>SCSITool</command> (de Hard+Soft GmBH).

<orderedlist>
<listitem><para>

Inicie <command>SCSITool</command> y elija el disco que desea particionar.
(menú <guimenu>Disk</guimenu> menú, opción <guimenuitem>select</guimenuitem>).

</para></listitem>
<listitem><para>

Desde el menú <guimenu>Partition</guimenu>, elija ya sea 
<guimenuitem>New</guimenuitem> para añadir nuevas particiones
o cambiar el tamaño de las existentes o <guimenuitem>Change</guimenuitem>
para cambiar una partición en específico. A menos que ya tenga
creadas las particiones con los tamaños correctos y solamente quiera
cambiar el ID de la partición, probablemente <guimenuitem>New</guimenuitem>
sea la mejor opción.

</para></listitem>
<listitem><para>

En caso use <guimenuitem>New</guimenuitem>, elija <guilabel>existing</guilabel>
en la caja que diálogo que se muestra en la configuración inicial. La 
ventana siguiente muestra una lista de las particiones existentes las
cuales puede ajustar usando los botones de desplazamiento, o presionando
el botón del ratón en los gráficos de barras. La primera columna en la
lista de particiones es el tipo de partición, simplemente presione en el
siguiente campo para editarlo. Cuando haya finalizado el cambiar la
configuración de particiones, guarde los valores actuales cerrando
la ventana al presionar el botón <guibutton>OK</guibutton>.

</para></listitem>
<listitem><para>

En caso use la opción <guimenuitem>Change</guimenuitem>, elija la
partición a cambiar en la lista de selecciones y elija <guilabel>other
systems</guilabel> en la caja de diálogo. La siguiente ventana muestra
información detallada sobre la ubicación de la partición y le permite
cambiar el ID de ésta partición. Guarde los valores cerrando la
ventana al presionar el botón <guibutton>OK</guibutton>.

</para></listitem>
<listitem><para>

Escriba los nombre de cada partición que ha creado o cambiado para
usarlo con Linux &mdash; vea <xref linkend="device-names"/>.

</para></listitem>
<listitem><para>

Cierre <command>SCSITool</command> usando la opción 
<guimenuitem>Quit</guimenuitem> desde el menú <guimenu>File</guimenu>.
El computador se reiniciará para asegurarse que la tabla de particiones
modificada sea usada por TOS. Si usted cambió cualquier partición TOS/GEM,
estas serán invalidadas y tendrán que ser reinstaladas (le advertimos
que haga copias de seguridad ¿cierto?).

</para></listitem>
</orderedlist>

</para><para>

Existe una herramienta de particionado para Linux/m68k llamada
<command>atari-fdisk</command> en el sistema de instalación, pero por
ahora le recomendamos particionar su disco usando el editor de particiones
de TOS o alguna herramienta de disco. Si su editor de particiones no tiene
una opción para editar el tipo de partición, usted puede efectuar este
paso crucial en mayor tiempo (desde un disco de instalación usando RAMdisk).
<command>SCSITool</command> solamente es uno de los editores de partición
que conocemos, el cual soportar la selección arbitraria de tipos de partición.
Podrían haber otros, elija la herramienta que se adapte a sus necesidades.

</para>
</sect2>

  <sect2 arch="m68k"><title>Particionar en MacOS</title>
<para>

Las herramientas de particionado probadas, para Macintosh, incluyen
<command>pdisk</command>, <command>HD SC Setup</command> 7.3.5 (Apple), 
<command>HDT</command> 1.8 (FWB), <command>SilverLining</command> (LaCie)
y <command>DiskTool</command> (Tim Endres, GPL). Se requiere las 
versiones completas para <command>HDT</command> y 
<command>SilverLining</command>. La herramienta de Apple requiere un
parche para que puede reconocer discos de terceros (puede encontrar
una descripción de como parchar <command>HD SC Setup</command> en
<ulink url="http://www.euronet.nl/users/ernstoud/patch.html"></ulink>).

</para><para>

Para Macs basadas en IDE, necesita usar <command>Apple Driver Setup</command>
para crear el espacio libre para las particiones Linux y completar el
particionado en Linux o usar la versión de MacOS

</para>
</sect2>

