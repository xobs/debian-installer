<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 11648 -->

 <sect1 id="pre-install-bios-setup">
 <title>Pre-instalación de hardware y configuración del sistema operativo</title>
<para>

Esta sección le guiará para una pre-instalación de hardware, de ser
necesario, que necesite para realizar la instalación de Debian. 
Generalmente, esto involucra verificar y posiblemente cambiar la
configuración del firmware para su sistema. El ``firmware'' es la base
de software usada por el hardware; es críticamente invocada durante
el proceso de arranque (despues del encendido). También son
remarcados los problemas de hardware conocidos que afectan la 
confiabilidad de &debian; en su sistema.

</para>

&bios-setup-i386.xml; 
&bios-setup-m68k.xml; 
&bios-setup-powerpc.xml; 
&bios-setup-sparc.xml; 
&bios-setup-s390.xml; 

  <sect2><title>Problemas de hardware a tener en cuenta</title>
<para arch="not-s390">

Muchas personas han intendado hacer funcionar su sistema de 90 MHz a 100 Mhz,
etc. Esto usualmente funciona, pero es sensible a la temperatura y otros
factores y de hecho podría dañar su sistema. Uno de los autores de
este documento forzó la velocidad de su sistema durante un año y entonces
el sistema empezó a abortar el programa <command>gcc</command> con una
señal inesperada mientras estaba compilando el núcleo del sistema operativo.
El cambiar la velocidad del procesador a su valor original solucionó 
el problema.

</para><para arch="not-s390">

El compilador <command>gcc</command> frecuentemente es la primera
cosa que falle debido a módulos de memoria defectuosos (u otros
problemas de hardware relacionados que pueden cambiar la información
impredeciblemente) debido a que construyen grandes estructuras de 
datos que lo sobrepasa repetidamente. Un error en estas estructuras
de datos causará una ejecución de una instrucción ilegal o acceso
a una dirección no existente. El síntoma de esto será que 
<command>gcc</command> falle con una señal inesperada.

</para><para arch="m68k">

Las placas de RAM de Atari TT son notables por los problemas de RAM
en Linux, si usted encuentra cualquier problema extraño intente ejecutar
al menos el núcleo en ST-RAM. Los usuarios de Amiga podrían necesitar
excluir la RAM usando un fichero de memoria de arranque.

<phrase condition="FIXME"><emphasis>

FIXME: more description of this needed.

</emphasis></phrase>

</para><para arch="i386">

Las mejores placas base soportan RAM con paridad y le avisarán si se produce
un error de bit simple en la RAM de su sistema. Desafortunadamente, no
disponen de un método de corrección del error, y generalmente dejan de
funcionar inmediatamente después de la notificación del error. Aún así, es
mejor ser avisado de la memoria defectuosa que insertar errores 
inadvertidamente en sus datos. De este modo, los mejores sistemas tienen
placas base que soportan módulos con paridad y con paridad real; vea 
<xref linkend="Parity RAM"/>.

</para><para arch="i386">

Si usted tiene RAM de paridad real y su placa base puede manejarla,
asegúrese de habilitar cualquier configuración del BIOS que pueda causar
la interrupción de la placa base al producirse errores de paridad de
memoria.

</para>

   <sect3 arch="i386"><title>El interruptor Turbo</title>
<para>

Muchos sistemas tiene un interruptor <emphasis>turbo</emphasis> que
controla la velocidad del CPU. Elija la configuración de alta 
velocidad. Si su BIOS le permite deshabilitar el control desde
software del interruptor turbo (o control desde software de la
velocidad del procesador), hágalo y bloquee el sistema en el modo
de alta velocidad. Tenemos conocimiento que en un sistema en particular,
mientras Linux esta auto probando (buscando los dispositivos de
hardware) accidentalmente puede tocar el software de control del
interruptor turbo.

</para>
   </sect3>

   <sect3 arch="i386"><title>Procesadores Cyrix y errores de disquete</title>
<para>

Algunos usuarios de procesadores Cyrix han tenido que deshabilitar la caché 
de sus sistemas durante la instalación, puesto que los disquetes producen 
errores si ésta no está deshabilitada. Si tiene que hacerlo, asegúrese de
rehabilitar su caché cuando haya completado la intalación, puesto que el
sistema funciona <emphasis>mucho</emphasis> más lento con la caché 
deshabilitada.

</para><para>

No creemos que el fallo está necesariamente en el procesador Cyrix. Puede 
ser algo que Linux puede evitar. Continuaremos trabajando en el problema.
Para los técnicamente curiosos, sospechamos un problema con la caché al
pasar de código de 16 a 32 bits.

</para>
   </sect3>

   <sect3 arch="i386"><title>Configuración de hardware de periféricos</title>
<para>

Podría necesitar cambiar algunas configuraciones o jumpers en las
tarjetas de los periféricos en su computador. Algunas tarjetas tiene
menus de configuración, mientras que otras confían en los jumpers.
Este documento no puede pretender proveer información completa
sobre cada dispositivo de hardware; lo que si espera es proveer
consejos de gran ayuda.

</para><para>

Si su tarjeta provee ``mapped memory'', la memoria debe ser
mapeada en algún lugara entre 0xA0000 y 0xFFFF (desde 640K hasta
debajo de 1 megabyte) o en alguna dirección de al menos 1 megabyte
superior al total de la RAM instalada en su sistema.

</para>
   </sect3>

   <sect3 arch="i386" id="usb-keyboard-config"><title>Teclados USB</title>
<para>

Si usted no tiene teclados de estilo AT y solamente model USB, 
necesitará habilitar la emulación de compatibilidad de teclado
en la configuración del BIOS. Consulte el manual de su placa base
y busque las opciones "Emulación de compatibilidad de teclado" o 
"Soporte de teclado USB". Deberá ser habilitado de modo que el
sistema de instalación pueda arrancar. Si ha habilitado esta opción
y funciona, prosiga con los siguientes pasos.

</para><para>

Si no puede encontrar esta opción, podría ser que este siempre 
habilitada de modo que puede proseguir. También podría ser que el
BIOS no provee soporte de emulación (en este caso mala suerte).

</para><para>

Si encuentra la opción y la habilita, pero la emulación deja de
trabajar tan pronto como el núcleo es iniciado, entonces también
tiene mala suerte. Deberá intentar la variante "bf2.4" donde el
disquete root tiene los módulos USB. Si esta instalando con
disquetes, podría necesitar el teclado después que los módulos
USB se carguen. Especificando la opción "keytimer" en el cursor
de arranque podría ser de ayuda en este caso.

</para><para>

Alguna veces, la emulación deja de funcionar pero vuelve a hacerlo
después de algunos minutos, de modo que usted puede esperar un poco
e intentar proseguir. Para corregir este comportamiento, deberá
cargar los controladores propios de Linux para teclados USB,
Para esto use "modconf" (Paso "Configurar módulos controladores
de dispositivos") y cargue los módulos usb-uhci o usb-ohci.

</para>
   </sect3>

   <sect3><title>Más de 64 MB de RAM</title>
<para>

El núcleo Linux no siempre puede detectar el tamaño total de la RAM 
instalada en su sistema. Si este es su caso, por favor revise
<xref linkend="boot-parms"/>.

</para>
   </sect3>
  </sect2>
 </sect1>
