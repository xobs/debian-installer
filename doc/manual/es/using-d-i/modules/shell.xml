<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- retain these comments for translator revision tracking -->
<!-- original version: 21579 -->

   <sect3 id="shell">
   <title>Uso de la shell y revisión de los registros</title>
   <!-- TODO: There is nothing about logs in this section! -->

<para>

Hay una opción en el menú denominada <guimenuitem>Ejecutar una
shell</guimenuitem>. Si la opción no está disponible cuando necesite usar la
shell, presione <keycombo><keycap>Alt izquierdo</keycap>
<keycap>F2</keycap></keycombo> (en un teclado Mac,
<keycombo><keycap>Opción</keycap> <keycap>F2</keycap> </keycombo>) para cambiar
a la segunda <emphasis>consola virtual</emphasis>. Esto es, la tecla
<keycap>Alt</keycap> a la izquierda de la <keycap>barra espaciadora</keycap>, y
la tecla de función <keycap>F2</keycap>, al mismo tiempo. Verá una ventana a
parte ejecutando un clon de la shell Bourne llamado <command>ash</command>.

</para><para>

En este momento ha arrancado desde un disco en memoria RAM, y dispone de un
número limitado de utilidades Unix para usar. Puede ver los programas
disponibles con la orden <command>ls /bin /sbin /usr/bin
/usr/sbin</command> y escribiendo <command>help</command>. El editor de textos
es <command>nano</command>. La shell tiene algunas características útiles, como
la autocompletación e historial.

</para><para>

Use los menús para realizar cualquier tarea que pueda realizar con
ellos. &mdash; la shell y lar órdenes están sólo para cuando las cosas no
funcionen. En particular, debe usar siempre los menús y no la shell para activar
la partición swap, porque el programa del menú no puede detectar que haya hecho
esto desde la shell. Presione <keycombo><keycap>Alt izquierdo</keycap>
<keycap>F1</keycap></keycombo> para volver a los menús, o escriba
<command>exit</command> si usó una opción del menú para abrir la shell.

</para>
   </sect3>
