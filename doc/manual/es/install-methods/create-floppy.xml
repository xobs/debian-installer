<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 18623 -->

 <sect1 condition="supports-floppy-boot" id="create-floppy">
 <title>Crear disquetes desde imágenes de disco</title>
<para>

Los disquetes arrancables son generalmente usados como último recurso
para arrancar el instalador en hardware que no puede hacerlo desde CD
o por otros métodos.

</para><para arch="powerpc">

El arranque desde disquete ha sido reportado como fallido en unidades
de disquete USB de Mac.

</para><para arch="m68k">

El arranque desde disquete no esta soportado en Amigas o Macs 68k.

</para><para>

Las imágenes de disco son ficheros que contienen el contenido completo
de un disquete en formato <emphasis>raw</emphasis>. Las imágenes de disco,
como <filename>boot.img</filename>, no pueden ser copiadas simplemente
a la unidad de disquete. Se usa un programa especial para escribir los
ficheros de imagen a la unidad de disquete en modo <emphasis>raw</emphasis>.
Esto se requiere debido a que estas imágenes son representaciones en "bruto"
del disco; se requiere efectuar una <emphasis>copia por sector</emphasis> 
de la data del fichero en el disquete.

</para><para>

Existen diferentes métodos para crear disquetes desde imágenes de disco,
las cuales dependen de su plataforma. Esta sección describe como crear
disquetes desde imágenes de disco en diferentes plataformas.

</para><para>

No importa el método que use para crear sus disquetes, deberá 
recordar activar la protección de escritura en los disquetes, una
vez que haya escrito en ellos, para asegurarse que no se dañen
sin intención.

</para>

  <sect2><title>Escribir imágenes de disco desde un sistema Linux o Unix</title>
<para>

Para escribir los ficheros de imagen de disco en los disquetes, probablemente
necesitará acceso de superusuario en el sistema. Coloque un disquete en blanco
y en buen estado en la unidad de disquetes. Seguidamente use la orden

<informalexample><screen>
dd if=<replaceable>fichero</replaceable> of=/dev/fd0 bs=1024 conv=sync ; sync
</screen></informalexample>

donde <replaceable>fichero</replaceable> es uno de los ficheros de imagen
de disco (vea <xref linkend="downloading-files"/> para saber que
<replaceable>ficheros</replaceable> deben ser).
<filename>/dev/fd0</filename> es el nombre usualmente usado para la unidad
de disquetes, podría ser distinto en su ordenador
<phrase arch="sparc">(en Solaris, es
<filename>/dev/fd/0</filename>)</phrase>.
La orden podría retornar el cursor antes de que Unix haya finalizado la
escritura en el disquete, por lo que espere que la luz de "disco en uso" en
la unidad de disquete este apagada y que ha finalizado de dar vueltas 
antes de retirarlo de la unidad. En algunos sistemas, tendrá que ejecutar
una orden para expulsar el disquete de la unidad
<phrase arch="sparc">(en Solaris, use <command>eject</command>, vea
la página de manual)</phrase>.

</para><para>

Algunos sistemas intentan montar automáticamente el disquete cuando
lo coloca en la unidad. Deberá deshabilitar esta característica antes
de que su ordenador le permita escribir en <emphasis>modo raw</emphasis>.
Desafortunadamente, la forma de hacer esto variará basado en su sistema
operativo.
<phrase arch="sparc">
En Solaris, puede modificar el administrador de volúmenes para tener
acceso raw al disquete. Primero, asegúrese de que el disquete ha sido
automontado (usando <command>volcheck</command> o la orden
equivalente en el gestor de ficheros). Luego use la orden
<command>dd</command> de la forma señalada a continuación, simplemente
reemplace <filename>/dev/fd0</filename> con
<filename>/vol/rdsk/<replaceable>nombre_disquete</replaceable></filename>,
donde <replaceable>nombre_disquete</replaceable> es el nombre que se ha
asignado al disquete cuando fue formateado (los disquetes no nombrados
tienen como nombre predeterminado <filename>unnamed_floppy</filename>).  
En otros sistemas, pregunte a su administrador
</phrase>

</para><para arch="powerpc"> 

Si escribe un disquete en Linux powerpc, necesitará expulsarlo. El
programa <command>eject</command> controla esto adecuadamente; podría
necesitar instalarlo.

</para>

  </sect2>

&floppy-i386.xml; <!-- can be used for other arches -->
&floppy-m68k.xml;
&floppy-powerpc.xml;

 </sect1>

