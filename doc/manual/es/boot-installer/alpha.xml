<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 19466 -->

 <sect2 arch="alpha" id="alpha-firmware">
 <title>Firmware de consola Alpha</title>
<para>

El firmware de consola está almacenado en una memoria flash ROM y
se inicia cuando se enciende o reinicia un sistema Alpha. Existen
dos diferentes modelos de especificación de consola usados en los
sistema Alpha, y por lo tanto dos clases de firmware de consola
disponibles:

</para>

<itemizedlist>
<listitem><para>

  <emphasis>Consola SRM</emphasis>, basada en la especificación 
  Alpha Console Subsystem, la cual provee un entorno operativo para
  los sistemas operativos OpenVMS, Tru64, UNIX y Linux.

</para></listitem>
<listitem><para>

  <emphasis>Consola ARC, AlphaBIOS o ARCSBIOS</emphasis>, basada en la
  especificación Advanced RISC Computing (ARC), la cual provee un
  entorno operativo para Windows NT.

</para></listitem>
</itemizedlist>

<para>

Desde la perspectiva del usuario, la diferencia más importante entre
SRM y ARC es que la elección del tipo de la consola restringe el posible
esquema de particionado para el disco duro del cual desea arrancar.

</para><para>

ARC requiere usar una tabla de particiones MS-DOS (como la que crea
<command>cfdisk</command>) para el disco de arranque. Por consiguiente
las tablas de particiones MS-DOS son el formato «nativo» de partición
cuando se arranca desde ARC. De hecho, puesto que AlphaBIOS contiene
una utilidad de particionado de disco, podría preferir particionar
sus discos desde los menús del firmware antes de instalar Linux.

</para><para>

Contrariamente, SRM es <emphasis>incompatible</emphasis> con tablas de
partición MS-DOS. 
<footnote><para>Específicamente, el formato del sector de arranque 
requerido por la especificación del subsistema de la consola entra en
conflicto con la ubicación de la tabla de particiones DOS.
</para></footnote> Puesto que Tru64 Unix usa el formato de etiquetas de
disco de BSD, este es el formato de partición «nativo» para instalaciones
SRM.

</para><para>

Debido a que GNU/Linux es el único sistema operativo en Alpha que 
se puede arrancar desde ambos tipos de la consola, la elección 
también dependerá de que otros sistemas operativos desea ejecutar
en la misma máquina. Todos los otros sistemas operativos de tipo
Unix (Tru64 Unix, FreeBSD, OpenBSD y NetBSD) y OpenVMS sólo pueden
arrancar desde SRM, mientras que Windows NT solo puede arrancar
desde ARC.

</para><para>

La siguiente tabla resume las combinaciones de sistema/consola
disponibles y soportadas (vea <xref linkend="alpha-cpus"/> para
saber los nombres de los tipos de sistema). Si la palabra «ARC» aparece
a continuación de un nombre, hace referencia a cualquiera de las consolas
compatibles con ARC.

</para><para>

<informaltable><tgroup cols="2">
<thead>
<row>
  <entry>Tipo de sistema</entry>
  <entry>Tipo de consola soportada</entry>
</row>
</thead>

<tbody>
<row>
  <entry>alcor</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>avanti</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>book1</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>cabriolet</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>dp264</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>eb164</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>eb64p</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>eb66</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>eb66p</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>jensen</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>lx164</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>miata</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>mikasa</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>mikasa-p</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>nautilus</entry>
  <entry>solamente ARC (vea el manual de la placa base)</entry>
</row><row>
  <entry>noname</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>noritake</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>noritake-p</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>pc164</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>rawhide</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>ruffian</entry>
  <entry>solamente ARC</entry>
</row><row>
  <entry>sable</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>sable-g</entry>
  <entry>solamente SRM</entry>
</row><row>
  <entry>sx164</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>takara</entry>
  <entry>ARC o SRM</entry>
</row><row>
  <entry>xl</entry>
  <entry>solamente ARC</entry>
</row><row>
  <entry>xlt</entry>
  <entry>ARC o SRM</entry>
</row>

</tbody>
</tgroup>
</informaltable>

</para><para>

Generalmente, ninguna de estas consolas puede arrancar Linux directamente,
por lo que se requiere de la asistencia de un gestor de arranque 
intermedio. Existen dos principales gestores de arranque para Linux:
<command>MILO</command> y <command>aboot</command>.

</para><para>

<command>MILO</command> es de por sí una consola que reemplaza a ARC o SRM
en memoria. <command>MILO</command> puede arrancar tanto desde ARC y SRM,
y es la única manera de cargar Linux desde la consola ARC.
<command>MILO</command> es específico de plataforma (se requiere una versión
diferente de <command>MILO</command> para cada tipo de sistema) y existe
solamente para los sistemas para los cuales existe soporte ARC como se 
muestra en la tabla anterior. Véase también el (desafortunadamente
desactualizado) <ulink url="&url-milo-howto;">CÓMO de MILO</ulink>.

</para><para>

<command>aboot</command> es un gestor de arranque, pequeño, independiente
de plataforma, que sólo se puede ejecutar desde SRM. Vea (el también
desafortunadamente desactualizado) <ulink
url="&url-srm-howto;">COMO de SRM</ulink> para mayor información acerca
de <command>aboot</command>.

</para><para>

Por lo tanto, generalmente existen tres posibles escenarios, dependiendo
del firmware de la consola del sistema y de si <command>MILO</command> está
disponible o no:

<informalexample><screen>

SRM -> aboot
SRM -> MILO
ARC -> MILO

</screen></informalexample>

</para><para>

La placa base UP1000 (con nombre de subarquitectura 'nautilus') de
Alpha Processor, Inc. es diferente de todas las otras, ésta usa un
gestor de arranque de API específico que se ejecuta bajo el firmware
AlphaBIOS.

</para><para>

Debido a que <command>MILO</command> no está disponible para todos
los sistemas Alpha actualmente en producción (hasta febrero de 2000),
y debido a que ya no es necesario comprar licencias de OpenVMS o Tru64
Unix para tener firmware SRM en su Alpha antigua, es recomendable que
use SRM y <command>aboot</command> en nuevas instalaciones de GNU/Linux,
a menos que desee tener arranque dual con Windows NT.

</para><para>

La mayoría de AlphaServers y todos los productos actuales de servidor
y estación de trabajo contienen tanto SRM como AlphaBIOS en su
firmware. Para máquinas con «mitad de flash» como las diversas placas
de evaluación, es posible cambiar de una versión a otra al re-grabar
el firmware. También, una vez que sea instalado SRM, es posible
ejecutar ARC/AlphaBIOS desde un disquete (usando la orden 
<command>arc</command>). Por estas razones, recomendamos cambiarlo
a SRM antes de instalar &debian;.

</para><para>

Como en otras arquitecturas, debería instalar la revisión de firmware
más reciente disponible. 
<footnote><para>Excepto en Jensen, donde Linux no está soportado
en versiones de firmware superiores a 1.7 - Vea
<ulink url="&url-jensen-howto;"></ulink> para mayor información
</para></footnote> antes de instalar &debian;.  Para Alpha, las
actualizaciones de firmware pueden obtenerse en
<ulink url="&url-alpha-firmware;">Actualizaciones de firmware de Alpha</ulink>.

</para>
 </sect2>


  <sect2 arch="alpha"><title>Arrancar con TFTP</title>
<para>

En SRM, las interfaces Ethernet se nombran con el prefijo
<userinput>ewa</userinput>, y se muestran con la orden
<userinput>show dev</userinput>, como este ejemplo
(editado un poco):

<informalexample><screen>

>>> show dev
ewa0.0.0.9.0               EWA0              08-00-2B-86-98-65
ewb0.0.0.11.0              EWB0              08-00-2B-86-98-54
ewc0.0.0.2002.0            EWC0              00-06-2B-01-32-B0

</screen></informalexample>

Primero debe configurar el protocolo de arranque:
<informalexample><screen>

>>> set ewa0_protocol bootp

</screen></informalexample>

Luego verificar que el tipo de medio sera correcto:

<informalexample><screen>

>>> set ewa0_mode <replaceable>modo</replaceable>

</screen></informalexample>

Puede obtener una lista de los modos válidos con <userinput>>>>set ewa0_mode</userinput>.

</para><para>

Entonces, para arrancar desde la primera interfaz Ethernet, debería
escribir:

<informalexample><screen>

>>> boot ewa0 -flags ""

</screen></informalexample>

</para><para>

Esto arrancará usando los parámetros predeterminados del núcleo
conforme están incluidos en la imagen de arranque de red.

</para><para>

Si desea usar la consola de serie, <emphasis>deberá</emphasis>
pasar el parámetro <userinput>console=</userinput> al núcleo.
Esto se puede hacer usando el argumento <userinput>-flags</userinput>
para la orden <userinput>boot</userinput> de SRM. Los puertos serie
se nombran de forma similar a sus ficheros correspondientes en
<userinput>/dev</userinput>. Por ejemplo, para arrancar desde
<userinput>ewa0</userinput> y usar una consola en el primer puerto
serie, debería escribir:

<informalexample><screen>

>>> boot ewa0 -flags console=ttyS0

</screen></informalexample>

</para>
  </sect2>

  <sect2 arch="alpha"><title>Arrancar desde CD-ROM con la consola SRM</title>
<para>

Escriba

<informalexample><screen>

&gt;&gt;&gt; boot xxxx -flags 0

</screen></informalexample>

donde <replaceable>xxxx</replaceable> es su unidad de CD-ROM en notación de SRM.

</para>
  </sect2>

  <sect2 arch="alpha">
  <title>Arranque desde CD-ROM con la consola ARC o AlphaBIOS</title>
<para>

Para arrancar un CD-ROM desde la consola ARC, localice el nombre en clave
de su subarquitectura (vea <xref linkend="alpha-cpus"/>), luego
introduzca <filename>\milo\linload.exe</filename> como gestor de 
arranque y <filename>\milo\<replaceable>subarq</replaceable></filename>
(donde
<replaceable>subarq</replaceable> es el nombre de subarquitectura apropiado)
como la ruta de sistema operativo en el menú «OS Selection Setup». Las
máquinas «ruffian» son una excepción: necesita usar 
<filename>\milo\ldmilo.exe</filename> como gestor de arranque.

</para>
  </sect2>


  <sect2 arch="alpha">
  <title>Arrancar desde disquetes con la consola SRM</title>
<para>

En el cursor de SRM (<prompt>&gt;&gt;&gt;</prompt>), escriba la
siguiente orden:

<informalexample><screen>

&gt;&gt;&gt; boot dva0 -flags 0
        
</screen></informalexample>

posiblemente reemplazando <filename>dva0</filename> con el nombre real
del dispositivo. Usualmente, <filename>dva0</filename> es la unidad
de disquete; escriba

<informalexample><screen>

&gt;&gt;&gt; show dev
        
</screen></informalexample>

para ver la lista de dispositivos (por ejemplo, si desea arrancar
desde un CD). Note que si está arrancando a través de MILO, el
argumento <command>-flags</command> no se tiene en cuenta, por lo que
simplemente puede escribir <command>boot dva0</command>.
Si todo funciona correctamente, eventualmente verá el arranque
del núcleo Linux.

</para><para>

Si desea especificar parámetros de núcleo específicos cuando arranque
a través de <command>aboot</command>, use la siguiente orden:
<informalexample><screen>

&gt;&gt;&gt; boot dva0 -file linux.bin.gz -flags "root=/dev/fd0 load_ramdisk=1 argumentos"
        
</screen></informalexample>
(escrita en una sola línea). Substituya, en caso de que sea necesario, el 
nombre real del dispositivo de arranque SRM por <filename>dva0</filename>, el
nombre del dispositivo de arranque de Linux por <filename>fd0</filename>, y
los parámetros de núcleo deseados por <filename>argumentos</filename>.

</para><para>

Si desea especificar parámetros de núcleo cuando arranque a través de
<command>MILO</command>, tendrá que interrumpir la carga cuando acceda
a MILO. Vea <xref linkend="booting-from-milo"/>.
</para>
  </sect2>


  <sect2 arch="alpha">
  <title>Arrancar desde disquetes con la consola ARC o AlphaBIOS</title>

<para>

En el menú «OS Selection», configure <command>linload.exe</command> como
el gestor de arranque, y <command>milo</command> como la ruta de sistema
operativo. Arranque usando la nueva entrada creada.

</para>
  </sect2>

  <sect2 arch="alpha">
  <title>Arranque desde disquetes con el gestor de arranque APB (UP1000)</title>

<para>

Para arrancar en esta plataforma, ejecute <command>\apb\apb.exe</command> 
desde el menú «Utility/Run Maintenance Program», y escriba
      <informalexample><screen>

boot debian_install
      
</screen></informalexample>
 en el cursor de APB.

</para>
  </sect2>

 <sect2 arch="alpha" id="booting-from-milo"><title>Arrancar con MILO</title>
<para>

MILO contenido en el medio de arranque esta configurado para proceder
directamente hacia Linux. Si desea intervenir, todo lo que necesita es
presionar espacio durante la cuenta atrás de MILO.

</para><para>

Si desea especificar todos los argumentos explícitamente (por ejemplo,
proveer parámetros adicionales, puede usar una orden como la siguiente:

<informalexample><screen>

MILO&gt; boot fd0:linux.bin.gz root=/dev/fd0 load_ramdisk=1 <!-- arguments -->
        
</screen></informalexample>

</para><para>

Si está arrancando desde otro dispositivo que no sea un disquete, sustituya
<filename>fd0</filename> en el ejemplo anterior, por el nombre de dispositivo
apropiado en la notación de Linux. La orden <command>help</command> le
dará una referencia resumida de la orden MILO.

</para>
 </sect2>

