<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 20466 -->

 <sect1 id="boot-troubleshooting">
 <title>Resolución de problemas y proceso de instalación</title>
<para>
</para>

  <sect2 id="unreliable-floppies">
  <title>Confiabilidad de los disquetes</title>

<para>

El mayor problema para las personas que instalan Debian por primera
vez parece ser la confiabilidad de los disquetes.

</para><para>

El disquete de arranque es el que tiene los peores problemas, debido 
a que es leído directamente por el hardware, antes de que arranque 
Linux. A menudo, el hardware no lee tan confiablemente como lo hace el
controlador de disquetes de Linux, y podría parar sin mostrar ningún
mensaje de error si lee datos incorrectos. También podrían
haber fallos en el lector de disquetes los cuales se indican por
medio de una gran cantidad de mensajes sobre errores E/S del
disquete.

</para><para>

Si su instalación se estanca o para en un disquete en particular,
lo primero que debe hacer es volver a descargar la imagen de disquete
y escribirla en un disquete <emphasis>distinto</emphasis>. El simple
reformateo del antiguo no es suficiente, incluso si es que aparentemente
el disquete ha sido formateado y escrito sin fallos. Muchas veces
es útil intentar escribir el disquete en un sistema diferente.

</para><para>

Un usuario informa que tuvo que escribir las imágenes a disquetes
<emphasis>tres</emphasis> veces antes que uno funcione y luego
no tuvo problemas con el tercer disquete.

</para><para>

Otros usuarios han informado que simplemente el reiniciar algunas 
veces con el mismo disquete en la unidad de disquete puede llevar
a un arranque exitoso. Esto se debe al hardware con fallos o los
controladores de firmware de la unidad.

</para>
  </sect2>

  <sect2><title>Configuración de arranque</title>

<para>

Si ocurren problemas y el núcleo colapsa durante el proceso de
arranque, no reconoce los dispositivos que tiene o las unidades
no son reconocidas adecuadamente, entonces lo primero que debe
verificar son los parámetros de arranque, como se explica en
<xref linkend="boot-parms"/>.

</para><para>

Si esta arrancando con su propio núcleo en lugar de que provee
el instalador, asegúrese de que la opción <userinput>CONFIG_DEVFS</userinput>
se haya configurado en su núcleo. El instalador requiere
<userinput>CONFIG_DEVFS</userinput>.

</para><para>

A menudo, los problemas se pueden solucionar al desconectar los
periféricos y accesorios, y luego intentar arrancar nuevamente.
<phrase arch="i386">Los módems internos, tarjetas de sonido, y 
los dispositivos "Plug-n-Play" pueden ser especialmente problemáticos.
</phrase>

</para><para>

Si tiene una buena cantidad de memoria en su máquina, más de 512MB, y
el instalador colapsa cuando arranca el núcleo, podría necesitar 
incluir un argumento de arranque para limitar la cantidad de 
memoria que reconocerá el núcleo, tal como 
<userinput>mem=512m</userinput>.

</para>
  </sect2>

  <sect2 id="kernel-msgs">
  <title>Interpretar los mensajes de inicio del núcleo</title>

<para>

Durante la secuencia de arranque podría ver muchos mensajes
de la forma
<computeroutput>can't find <replaceable>algo</replaceable>
</computeroutput>, o <computeroutput>
<replaceable>algo</replaceable> not present</computeroutput>,
<computeroutput>can't initialize <replaceable>algo</replaceable>
</computeroutput>, o incluso <computeroutput>this driver release depends
on <replaceable>algo</replaceable> </computeroutput>.
Muchos de estos mensajes no son graves. Son mostrados debido a que
el sistema de instalación esta construido para ejecutarse en ordenadores
con diversos dispositivos. Obviamente, ninguno de ellos tendrá cada uno
de todos los posibles dispositivos, de modo que el sistema operativo 
emite algunos mensajes de advertencia mientras intenta buscar dispositivos
que usted no tiene. Podría observar que el sistema hace una pausa por
unos momentos. Esto ocurre debido a que está esperando que un dispositivo
responda, y éste no esta presente en su sistema. Si encuentra que
el tiempo que toma el arrancar el sistema es inaceptablemente amplio,
puede crear un núcleo adaptado después (vea <xref linkend="kernel-baking"/>).

</para>
  </sect2>


  <sect2 id="problem-report">
  <title>Informar fallos</title>
<para>

Si ha pasado la fase inicial de arranque pero no puede completar
la instalación, la opción de informe de fallos podría ser útil.
Ésta copia los registros de fallos de sistema y la información de 
configuración a un disquete que deberá colocar en la unidad.
Esta información puede darle pistas sobre lo que ha fallado y como
solucionarlo. Si está enviando un informe de fallos podría querer
adjuntar esta información.

</para><para>

Otros mensajes de instalación pertinentes pueden ser
encontrados en <filename>/var/log/</filename> durante la instalación,
y <filename>/var/log/debian-installer/</filename> después de
que el ordenador ha sido arrancado con el sistema instalado.

</para>
  </sect2>

  <sect2 id="submit-bug">
  <title>Enviar informes de instalación</title>
<para>

Si todavía tiene problemas, por favor envíe un informe de instalación.
Le animamos a que lo haga incluso si la instalación es exitosa, de
modo que podamos obtener toda la información posible sobre la
mayor cantidad de configuraciones de hardware. Por favor use esta
plantilla cuando haga un informe de instalación, y envíelo como
un informe de fallo para el pseudo paquete «installation-reports»
enviándolo a <email>submit@bugs.debian.org</email>.

<informalexample><screen>

Package: installation-reports
Debian-installer-version: &lt;Escriba la fecha y desde donde obtuvo la imagen&gt; 
uname -a &lt;El resultado de ejecutar uname -a en el cursor de shell&gt;
Date: &lt;Fecha y hora de la instalación&gt;
Method: &lt;¿Cómo lo instaló? ¿Qué arrancó? Si fue una instalación a través 
        de red, ¿desde donde? ¿utilizó un servidor proxy?&gt;
Machine: &lt;Descripción de la máquina (ejm: IBM Thinkpad R32)&gt;
Processor: &lt;Tipo de procesador&gt;
Memory: &lt;Cantidad de memoria RAM&gt;
Root Device: &lt;¿IDE o SCSI? Tipo del dispositivo raíz&gt;
Root Size/partition table: &lt;Puede incluir su tabla de particiones,
     incluyendo notas que indiquen que particiones se montan y donde.&gt;
Output of lspci and lspci -n: &lt;Salida de la ordenes «lspci» y «lspci -n»&gt;

Base System Installation Checklist: &lt;Marque según sea su caso: Escriba «O»
     si dicha fase funcionó, «E» si presentó fallo y dejela en blanco si no
     intentó o usó esta opción.&gt;
[O] = OK, [E] = Error (describalo a continuación), [ ] = didn't try it

Initial boot worked:    [ ] &lt;¿Funcionó el arranque inicial?&gt;
Configure network HW:   [ ] &lt;¿Se configuró el hardware de red?&gt;
Config network:         [ ] &lt;¿Se configuró la red?&gt;
Detect CD:              [ ] &lt;¿Se detectó la unidad de CD?&gt;
Load installer modules: [ ] &lt;¿Se cargaron los módulos del instalador?&gt;
Detect hard drives:     [ ] &lt;¿Se detectaron los discos duros?&gt;
Partition hard drives:  [ ] &lt;¿Se particionó el disco duro?&gt;
Create file systems:    [ ] &lt;¿Se creó los sistemas de ficheros?&gt;
Mount partitions:       [ ] &lt;¿Se montaron las particiones?&gt;
Install base system:    [ ] &lt;¿Se instaló el sistema base?&gt;
Install boot loader:    [ ] &lt;¿Se instaló el gestor de arranque?&gt;
Reboot:                 [ ] &lt;¿Reinició correctamente?&gt;

Comments/Problems:

&lt;Describa la instalación en detalle, e incluya cualquier idea o
comentario que tuvo durante la instalación.&gt;

</screen></informalexample>

Nota de traductor: Los comentarios (todo lo que va entre &lt; &gt;) en español
deben ser eliminados en el momento de llenar el informe. No debe traducir o
escribir frases en español en las opciones especificadas.

</para><para>

En el informe de fallo describa cual es el problema, incluya los
últimos mensajes visibles del núcleo en el caso de que éste
colapse. Describa los pasos que realizo y que le condujeron a
la situación del fallo.

</para>

  </sect2>

 </sect1>
