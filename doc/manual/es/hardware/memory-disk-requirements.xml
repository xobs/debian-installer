<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 18612 -->

 <sect1 id="memory-disk-requirements">
 <title>Requerimientos de memoria y espacio en disco</title>

<para>

Debe tener al menos &minimum-memory; de memoria y &minimum-fs-size;
espacio de disco duro. Para un sistema mínimo basado en consola (todos los
paquetes estándar), se requiere 250MB. Si quiere instalar una cantidad 
razonable de software, incluyendo el sistema de ventanas X, y algunos 
programas de desarrollo y bibliotecas, necesitará al menos 400MB. Para 
una instalación más o menos completa de un sistema de escritorio,
necesitará unos cuantos gigabytes.

</para><para arch="m68k"> 

En el Amiga el tamaño de la FastRAM es relevante frente a los
requerimientos totales de memoria. Además, el uso de tarjetas Zorro
con RAM de 16-bit no está soportado; necesitará RAM de 32-bit. El 
programa <command>amiboot</command> puede usarse para deshabilitar la RAM de 
16-bit; vea <ulink url="&url-m68k-faq;">Linux/m68k FAQ</ulink>. Los 
núcleos actuales deberían desactivar la RAM de 16-bit automáticamente.

</para><para arch="m68k">

En los Atari, tanto la ST-RAM como la Fast RAM (TT-RAM) son usadas por
Linux. Muchos usuarios han informado de problemas al ejecutar el núcleo
con Fast RAM, por lo que el sistema de arranque de Atari cargará el núcleo 
en ST-RAM. Los requerimientos mínimos de ST-RAM son de 2 MB.

</para><para arch="m68k">

En los Macintosh, debe tomarse cuidado con las máquinas con el vídeo
basado en RAM (RBV). El segmento de RAM en la dirección física 0 se usa
como memoria de pantalla, haciendo que la posición de carga predeterminada 
del núcleo no este disponible. El segmento de RAM alternativo usado por el
núcleo y el disco ram debe ser de al menos 4 MB.

</para><para arch="m68k">

<emphasis condition="FIXME">FIXME: ¿esto es todavía cierto?</emphasis>

</para>

 </sect1>
