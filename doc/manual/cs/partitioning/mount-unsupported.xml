<?xml version="1.0" encoding="ISO-8859-2"?>
<!-- $Id$ -->
<!-- original version: 1.3 -->

 <sect1 id="mount-other">
 <title>Připojení oblastí nepodporovaných programem
   <command>debian-installer</command>
 </title>

<para>

V některých speciálních případech se stane, že
<command>debian-installer</command> neví jak připojit souborový
systém (ať už kořenový nebo nějaký jiný). Pokud jste zkušený uživatel
GNU/Linuxu, můžete se jednoduše přepnout na druhou konzoli
(<userinput>tty2</userinput>) a připojit daný souborový systém ručně.

</para><para>

V případě, že připojujete kořenovou oblast svého nového systému,
připojte ji jako <filename>/target</filename>, vraťte se do
dbootstrapu a pokračujte.  (Neuškodí spuštění kroku <quote>Zobrazit
tabulku oddílů</quote>, aby se &d-i; zorientoval, v jaké části
instalačního procesu se nachází).

</para><para>

Pro nekořenové oblasti nesmíte zapomenout ručně změnit soubor
<filename>fstab</filename>, aby se oblasti po restartu správně
připojily. Před modifikací tohoto souboru
(<filename>/target/etc/fstab</filename>) musíte samozřejmě počkat, než
ho <command>debian-installer</command> vytvoří.

</para>
 </sect1>
