<?xml version="1.0" encoding="ISO-8859-2"?>
<!-- $Id$ -->
<!-- original version: 1.3 -->

 <sect1 id="init-partition">
 <title><quote>Inicializovat linuxovou oblast</quote></title>
<para>

Další položka menu bude <quote>Inicializovat linuxovou
oblast</quote>. Pokud tomu tak není, nedokončili jste rozdělení disku,
nebo jste vynechali něco v přípravě oddílu pro virtuální paměť.

</para><para>

Můžete inicializovat linuxový oddíl, nebo alternativně připojit
souborový systém inicializovaný dříve. Program
<command>debian-installer</command> <emphasis>neprovede</emphasis>
aktualizaci starší instalace, aniž by ji nepřepsal.
Chcete-li nedestruktivně přejít k novější verzi Debianu, použijte
příkaz <command>apt-get dist-upgrade</command> &mdash;
<command>debian-installer</command> vůbec nepotřebujete.

</para><para>

Pokud chcete pro instalaci použít část disku, která není prázdná, měli
byste ji inicializovat, čímž také smažete její dřívější obsah. Dále
musíte inicializovat oddíly, které jste vytvořili dříve při dělení
disku. Asi jediným důvodem pro volbu ,,mount a partition without
initializing it'' (připojit oddíl bez inicializace) může být pokračování
v nedokončené instalaci.

</para><para>

Zvolením položky <quote>Inicializovat linuxovou oblast</quote>
připravíte a připojíte diskový oddíl. První oddíl, který připojíte
nebo inicializujete, bude připojen jako <filename>/</filename> -
nazývá se <quote>root</quote> (hlavní, kořenový svazek souborů).

</para><para>

V tomto okamžiku budete dotázáni, jestli se má na oblasti provést test
na výskyt špatných bloků. Standardně se tato volba přeskakuje, protože
kontrola může zabrat dosti času a navíc se řadiče moderních disků o
špatné bloky starají samy. Pokud si nejste jisti kvalitou svého disku,
nebo máte starší počítač, je lepší tuto kontrolu provést.

</para><para>

Další dotazy jsou pouze potvrzovací. Protože inicializování oblasti
zničí všechna data na ní umístěná, dobře si odpověď rozmyslete.
Následně budete informováni, že kořenová oblast se připojuje jako
<filename>/</filename>.

<footnote><para>

Ve skutečnosti se připojí jako <filename>/target</filename>
a teprve po restartu do systému se připojí jako
<filename>/</filename>.

</para></footnote>

</para><para>

Jestliže máte další oblasti, které byste chtěli inicializovat a
připojit, můžete po připojení oddílu <filename>/</filename>, použít
alternativní menu.
Tento krok je pro ty, kdo si vytvořili samostatné oblasti pro
<phrase arch="not-powerpc"><filename>/boot</filename>,</phrase>
<filename>/var</filename>, <filename>/usr</filename> nebo jiné
adresáře. 

</para>
 </sect1>

