<?xml version="1.0" encoding="ISO-8859-2"?>
<!-- $Id$ -->
<!-- original version: 11648 -->

  <sect2 arch="ia64"><title>Dělení disku na &arch-title;</title>
<para>

ia64 EFI firmware podporuje dva formáty tabulky oddílů &mdash;
GPT a MS-DOS. Formát MS-DOS se typicky používá na počítačích i386 a
pro systémy ia64 se již nedoporučuje. Instalační systém nabízí dva
programy pro dělení disku &mdash;
<ulink url="cfdisk.txt"><command>cfdisk</command></ulink> a
<ulink url="parted.txt"><command>parted</command></ulink>.
Zatímco <command>parted</command> zvládá oba formáty tabulky oddílů,
<command>cfdisk</command> rozumí pouze formátu MS-DOS. Pokud jste měli
původně disk rozdělený tabulkou GPT a nyní chcete vytvořit tabulku 
oddílů ve formátu MS-DOS, je nezbytné použít program parted. Je to z
toho důvodu, že obě tabulky leží na jiném místě disku a cfdisk neví,
jak má tabulku GPT odstranit.

</para><para>

Důležitý rozdíl mezi oběma programy je také v tom, jak rozpoznávají
typ oblasti. <command>cfdisk</command> využívá bajtu v tabulce
rozdělení disku (například linuxová ext2 oblast má hodnotu 83),
zatímco <command>parted</command> zjišťuje typ oblasti prozkoumáním
dané oblasti. To například znamená, že dokud odkládací oblast (swap)
nenaformátujete, <command>parted</command> ji nerozpozná. Podobně
nebude považovat oblast za linuxovou ext2, dokud na ní nevytvoříte
souborový systém. <command>parted</command> umí vytvářet souborové
systémy a formátovat odkládací oblasti, takže byste toho měli využít.

</para><para>

Bohužel je <command>parted</command> ovládaný z příkazové řádky a tím
pádem se nepoužívá tak jednoduše jako <command>cfdisk</command>.
Předpokládejme, že chcete smazat celý disk a vytvořit tabulku GPT a
několik oblastí. Posloupnost příkazů by mohla vypadat nějak takto:

</para><para>
<informalexample><screen>

      mklabel gpt
      mkpartfs primary fat 0 50
      mkpartfs primary linux-swap 51 1000
      mkpartfs primary ext2 1001 3000
      set 1 boot on
      print
      quit

</screen></informalexample>
</para><para>

Tím se vytvoří nová tabulka rozdělení disku a tři oblasti pro použití
jako zaváděcí oblast EFI, virtuální paměť a kořenový souborový
systém. Nakonec se oblasti EFI nastaví příznak pro zavádění.
Oblasti se zadávají v megabajtech tak, že uvedete posunutí počátku
a konce oblasti od začátku disku. V příkladu jsme vytvořili oblast se
souborovým systémem ext2 o velikosti 1999MB, která začíná na
1001. megabajtu od začátku disku.
Poznámka: Formátování odkládací oblasti programem
<command>parted</command> může trvat několik minut, protože se
kontroluje výskyt vadných bloků. 

</para>
  </sect2>

  <sect2 arch="ia64"><title>Požadavky na zaváděcí oblast</title>

<para>

ELILO, zavaděč na ia64, vyžaduje oblast obsahující souborový systém
FAT. Jestliže jste použili oblasti GPT, měla by mít nastaven příznak
<userinput>boot</userinput>; pokud jste použili oblasti MS-DOS, pak by
měla být typu ,,EF''. Oblast musí být dostatečně velká, aby se do ní
vešel zavaděč, jádra a případné RAMdisky. Obvykle stačí 16MB, ale
jestli experimentujete s více jádry, klidně použijte oblast
o velikosti 128MB. 

</para>
  </sect2>
