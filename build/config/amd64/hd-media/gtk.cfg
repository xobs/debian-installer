TYPE = hd-media/gtk

EXTRANAME = gtk/

# Only build the gtk initrd and kernel, the bootable image is built by the
# main hd-media target and includes this initrd along with 2.6.
EXTRATARGETS = 
TARGET = $(KERNEL) $(INITRD)

INITRD_FS = initramfs

VIDEO_MODE=$(VIDEO_MODE_GTK)
