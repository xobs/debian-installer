SUBARCH_SUPPORTED = cdrom hd-media kernels nativehd 

# floppy and netboot aren't supported on amiga or mac, the only two rc subarchs.
#SUBARCH_SUPPORTED = cdrom floppy hd-media kernels nativehd netboot

INITRD_FS = ext2

arch_tree:

