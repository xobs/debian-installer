FLAVOUR_SUPPORTED = "" 2.6

# Not really a floppy, this is a 123 mb image, large enough to put a
# netinst iso in (currently..), and small enough to fit on a mid-range
# memory stick, such as those falsely advertised as being 128 mb in size.
FLOPPY_SIZE = 125952

DISK_LABEL = "bootable drive"
MEDIA_TYPE = bootable drive

GZIPPED = .gz
EXTRANAME = hd-media/

# Add the 2.6 images on to the bootable image.
EXTRATARGETS = build_hd-media_2.6
INITRD_2.6 = dest/hd-media/2.6/initrd.gz
KERNEL_2.6 = dest/hd-media/2.6/vmlinuz
UDEB_LISTS = $(TEMP_UDEB_LIST) tmp/hd-media_2.6/udeb.list
TARGET = $(KERNEL) $(INITRD) $(BOOT)

MANIFEST-BOOT = "123 mb image (compressed) for USB memory stick"
MANIFEST-INITRD = "for use on USB memory sticks"
MANIFEST-KERNEL = "for use on USB memory sticks"
