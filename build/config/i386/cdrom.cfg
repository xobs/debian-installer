# el-torito-2.6 is too large at the moment, so is disabled.
# isolinux-2.6 is disabled until all the 2.6 udebs are in the archive
FLAVOUR_SUPPORTED = el-torito isolinux #isolinux-2.6 #el-torito-2.6

MEDIA_TYPE = CD-ROM

# FIXME: Temporary fix, these files should be in a udeb!
EXTRALIBS = /lib/libnss_dns* /lib/libresolv*
