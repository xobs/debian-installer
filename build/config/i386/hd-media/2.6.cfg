KERNELVERSION = $(KERNELVERSION_2.6)
KERNELMAJOR = 2.6
EXTRANAME = 2.6/

# Only build the 2.6 initrd, the bootable image is built by the main
# hd-media target and includes this initrd along with 2.4.
EXTRATARGETS = 
TARGET = $(KERNEL) $(INITRD)
