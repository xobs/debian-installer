DISK_LABEL = "disk1:CD-ROM drivers floppy"

TARGET = $(EXTRA)
EXTRANAME = $(FLAVOUR)

IMAGE_SIZE = $(FLOPPY_SIZE)

MANIFEST-EXTRA = "CD drivers, including all IDE,PCMCIA drivers"

# No kernel.
KERNELNAME =

# HACK Alert: pkg-lists/ is still sorted by TYPE instead of a dir hierarchy.
TYPE=cd_drivers-1
DRIVER_FOR=floppy

INITRD_FS = ext2
