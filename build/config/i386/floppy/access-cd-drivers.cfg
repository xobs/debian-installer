DISK_LABEL = "Speakup CD driver module floppy"

KERNELVERSION = 2.4.27-speakup

IMAGE_SIZE = $(FLOPPY_SIZE)

TARGET = $(EXTRA)
EXTRANAME = access/cd-drivers

MANIFEST-EXTRA = "CD driver floppy for the blind"

# No kernel.
KERNELNAME =

# HACK Alert: pkg-lists/ is still sorted by TYPE instead of a dir hierarchy.
TYPE=access-cd-drivers
DRIVER_FOR=access
