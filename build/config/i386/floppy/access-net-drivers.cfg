DISK_LABEL = "Speakup network driver module floppy"

KERNELVERSION = 2.4.27-speakup

IMAGE_SIZE = $(FLOPPY_SIZE)

TARGET = $(EXTRA)
EXTRANAME = access/net-drivers

MANIFEST-EXTRA = "network driver floppy for the blind"

# No kernel.
KERNELNAME =

# HACK Alert: pkg-lists/ is still sorted by TYPE instead of a dir hierarchy.
TYPE=access-net-drivers
DRIVER_FOR=access
