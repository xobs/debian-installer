DISK_LABEL = "Speakup driver module floppy"

#KERNELVERSION = 2.4.26-speakup
KERNELVERSION = 2.4.24-speakup

IMAGE_SIZE = $(FLOPPY_SIZE)

TARGET = $(EXTRA)
EXTRANAME = access/drivers

MANIFEST-EXTRA = "driver floppy for the blind"

# No kernel.
KERNELNAME =

# HACK Alert: pkg-lists/ is still sorted by TYPE instead of a dir hierarchy.
TYPE=access_drivers
