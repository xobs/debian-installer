FLAVOUR_SUPPORTED = boot speakup root access access-net-drivers access-cd-drivers net-drivers cd-drivers

MEDIA_TYPE = floppy

# How big a floppy image should I make? (in kilobytes)
FLOPPY_SIZE = 1440

# media where images may be written to (via dd).
WRITE_MEDIA += $(FLAVOUR_SUPPORTED)
