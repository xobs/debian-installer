# This is just like the root floppy, except it has only Asian languages
# (and English) on it.
include config/powerpc/powerpc-small/floppy/root.cfg

# Using Perl negative lookahead.
DROP_LANG = '(?!zh_TW|zh_CN|ja|ko)'
DISK_LABEL = "root floppy (Asian languages)"
EXTRANAME = asian-
MANIFEST-ROOT = "second floppy for network or CD installs (with Asian languages)"
