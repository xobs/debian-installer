MEDIA_TYPE = netboot image

TARGET = $(INITRD) $(KERNEL) $(TEMP_BOOT)
EXTRANAME = $(MEDIUM)/

MANIFEST-BOOT = "tftp boot image for Power4 CPUs"
MANIFEST-INITRD = "initrd for use with power4 netboot"
MANIFEST-KERNEL = "kernel for use with power4 netboot"
