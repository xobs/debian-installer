KERNELVERSION = $(KERNELVERSION_2.6)
KERNELIMAGEVERSION = $(KERNELIMAGEVERSION_2.6)
KERNELMAJOR = 2.6
INITRD_FS = cramfs

EXTRATARGETS =
# Only build the temp files, do not install to dest, that is taken care of
# by the combined NETBOOT_DIR stuff.
TARGET = $(TEMP_KERNEL) $(TEMP_INITRD)
