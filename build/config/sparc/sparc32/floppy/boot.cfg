DISK_LABEL = "boot floppy"

TARGET = $(BOOT)

MANIFEST-BOOT = "boot floppy for network or CD installs (sparc32)"

# XXX: pkg-lists/ is still sorted by TYPE instead of a dir hierarchy.
TYPE = bootfloppy
