# This is just like the root floppy, except it has only cyrillic languages
# (and English) on it.
include config/sparc/sparc32/floppy-2.6/root.cfg
# Using perl negative lookahead.
DROP_LANG = '(?!ru|uk|bg|sr)'
DISK_LABEL = "root floppy (cyrillic languages, 2.6)"
EXTRANAME = cyrillic-
MANIFEST-ROOT = "second floppy for network or CD installs (with cyrillic languages, 2.6)"
