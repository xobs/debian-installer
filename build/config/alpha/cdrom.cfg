MEDIA_TYPE = CD-ROM

TARGET = $(INITRD) $(KERNEL)
EXTRANAME = $(MEDIUM)/

# FIXME: Temporary fix, these files should be in a udeb!
EXTRALIBS = /lib/libnss_dns* /lib/libresolv*

MANIFEST-KERNEL = "kernel for use with aboot to build a CD"
MANIFEST-INITRD = "initrd for use with aboot to build a CD"

