efi-reader

# This is dhcp client we're using on ia64, but it may vary for other
# arches, which is why it is not in common.
dhcp-client-udeb
#include "discover"

# net installation support
net-retriever
download-installer
netcfg
choose-mirror
hw-detect
ethdetect
