# Extra kernel-specific modules
#include "ia64-extra-modules"

# Commented efi-reader because the one in the archive is still linked
# against libdebian-installer3
#efi-reader
cdebconf-text-udeb
console-keymaps-at
console-keymaps-usb
discover1-data-udeb
discover1-udeb
kbd-chooser
usb-discover
ide-core-modules-${kernel:Version}
ide-modules-${kernel:Version}
scsi-modules-${kernel:Version} *
input-modules-${kernel:Version}
usb-modules-${kernel:Version}
usb-storage-modules-${kernel:Version}
cdrom-core-modules-${kernel:Version}
input-modules-${kernel:Version}

# Why nic modules on a CD?
nic-modules-${kernel:Version} *
