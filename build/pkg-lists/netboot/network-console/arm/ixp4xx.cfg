oldsys-preseed
devio-udeb
beep-udeb
input-modules-${kernel:Version}
nic-modules-${kernel:Version}
# In case people want to use a USB NIC instead of the IXP400 ethernet
# driver that requires proprietary microcode.
usb-modules-${kernel:Version}
nic-usb-modules-${kernel:Version}
# Remove localechooser to enable selection of a time zone when the
# clock is configured. For this to work, oldsys-preseed must not
# preseed the country (countrychooser/country-name).
localechooser -
