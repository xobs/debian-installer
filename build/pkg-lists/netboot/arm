console-keymaps-usb
console-keymaps-at

dhcp-client-udeb
# FIXME: currently contains fdisk-udeb as a workaround for anna not
# fetching it due to wrong dependencies/priorities, although it is
# needed by the partitioner.
# The entry will be removed as soon as the problem is fixed.
fdisk-udeb

kbd-chooser
#bogl-bterm-udeb
hw-detect
ethdetect
netcfg

# used by netcfg
libiw27-udeb

#kernel modules

#for all targets
socket-modules-${kernel:Version}

#netwinder
nic-shared-modules-${kernel:Version=netwinder}
isa-pnp-modules-${kernel:Version=netwinder}
nic-modules-${kernel:Version=netwinder}
nic-extra-modules-${kernel:Version=netwinder}
usb-modules-${kernel:Version=netwinder}



base-installer
