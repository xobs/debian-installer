#isa-pnp-modules-${kernel:Version}
ide-core-modules-${kernel:Version}
ide-modules-${kernel:Version}
cdrom-core-modules-${kernel:Version}
#cdrom-modules-${kernel:Version}
scsi-modules-${kernel:Version} *
scsi-common-modules-${kernel:Version}
usb-storage-modules-${kernel:Version}

# Here because it does not fit on the boot floppy.
countrychooser
iso-3166-udeb

# For pcmcia cdroms
pcmcia-storage-modules-${kernel:Version}
# The pcmcia support doesn't currently fit, and you can probably
# boot off of those anyway. Or use the net-drivers floppy too.
#pcmcia-modules-${kernel:Version}
#pcmcia-cs-udeb
