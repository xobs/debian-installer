acpi-modules-${kernel:Version}
socket-modules-${kernel:Version} ?
console-keymaps-at

# This is needed for proper display of utf-8.
fb-modules-${kernel:Version}

# This is dhcp client we're using on i386, but it may vary for other
# arches, which is why it is not in common.
dhcp-client-udeb

# net installation support
net-retriever
download-installer
netcfg
choose-mirror
hw-detect
ethdetect

# If the floppy gets full, move these to the net-drivers floppy.
wireless-tools-udeb

# Only here since we currently have the space; this would be the first
# thing to remove if the disk filled up again.
save-logs
mountfloppy

# TO BE REMOVED FROM THE LIST
# this is due to a bug 
# before mounting floppy, we have to remove and reinsert floppy module
floppy-modules-${kernel:Version}
