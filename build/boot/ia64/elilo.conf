#
# elilo.conf for netbooting ia64 client
# assumes a 9600 baud serial console, change or remove the console= if needed
#
default=Linux
  
image=ia64/vmlinuz
    label=Linux
    initrd=ia64/initrd.gz
    root=/dev/ram
    append="ramdisk_size=32768 console=ttyS0,9600n8"
    read-only
