# default64 does not currently behave right with vesamenu
#default64 amd64-install

label amd64-install
	menu label ^64 bit install
	kernel ${KERNEL}
	append ${VIDEO_MODE} initrd=${INITRD} -- quiet ${CONSOLE}
