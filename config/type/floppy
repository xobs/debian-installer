# How big a floppy image should I make? (in kilobytes)
FLOPPY_SIZE=1440

# Other things that need buildin
EXTRA_TARGETS=net_drivers

# Name of the driver1 floppy image to create
NETDRIVERS_IMAGE=$(DEST)/net_drivers-image.img

CDDRIVERS_IMAGE=$(DEST)/cd_drivers-image.img

EXTRA_IMAGES=$(NETDRIVERS_IMAGE)  

NETDRIVERS=$(TEMP)/net_drivers

CDDRIVERS=$(TEMP)/cd_drivers
