# The version of the kernel to use.
KERNELVERSION = 2.4.22-speakup
KERNEL_FLAVOUR = di
KERNELNAME = vmlinuz
KERNELIMAGEVERSION = $(KERNELVERSION)

DISK_LABEL = "boot floppy"

TARGET = $(BOOT)
EXTRANAME = access/

# HACK Alert: pkg-lists/ is still sorted by TYPE instead of a dir hierarchy.
TYPE=speakup
