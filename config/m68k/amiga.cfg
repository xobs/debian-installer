MEDIUM_SUPPORTED = nativehd

# The version of the kernel to use.
KERNELVERSION = 2.4.25
KERNEL_FLAVOUR = di
KERNELIMAGEVERSION = $(KERNELVERSION)-$(SUBARCH)
KERNELNAME = vmlinuz

USERDEVFS = t

TARGET = $(INITRD) $(KERNEL)
