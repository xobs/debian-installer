# The version of the kernel to use.

KERNELIMAGEVERSION=2.4.21-sparc32 2.4.21-sparc64
# Still using the old -udeb.udebs for now
KERNEL_FLAVOUR=udeb
KERNELVERSION=$(foreach VERSION,${KERNELIMAGEVERSION},$(VERSION))
KERNELNAME=$(foreach VERSION,${KERNELVERSION},vmlinux-$(VERSION))

USERDEVFS=t

INITRD_FS=romfs

TYPE = netboot
