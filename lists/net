# These udebs (plus base) are needed on a single-floppy system that
# can get up on the net and download more udebs. If it's not needed for
# such a system, do *not* list it here (space is tight..)
choose-mirror
wget-retriever
ethdetect
netcfg-static
netcfg-dhcp
dhcp-client-udeb
nic-modules-${kernel:Version}-${kernel:Flavour}-udeb
nic-modules-shared-${kernel:Version}-${kernel:Flavour}-udeb
isa-pnp-modules-${kernel:Version}-${kernel:Flavour}-udeb
modutils-basic
