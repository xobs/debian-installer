# These udebs (plus base) are needed on a single-floppy system that
# gets everything from the net via wget.
choose-mirror
wget-retriever
ethdetect
netcfg-static
#netcfg-dhcp
netkit-tiny-udeb
nic-modules-2.4.0-di
modutils-udeb
