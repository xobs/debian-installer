# These udebs (plus base) are needed on a single-floppy system that
# gets everything from the net via wget.
choose-mirror
wget-retriever
# ethdetect is too big at the moment
#ethdetect
niccfg-manual
netcfg-static
# dhcp udebs are not yet available
#netcfg-dhcp
netkit-tiny-udeb
nic-modules-2.4.1-di
isa-pnp-modules-2.4.1-di
modutils-basic
