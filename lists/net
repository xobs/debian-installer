# These udebs (plus base) are needed on a single-floppy system that
# gets everything from the net via wget.
choose-mirror
wget-retriever
ethdetect
netcfg-static
netcfg-dhcp
dhcp-client-udeb
netkit-tiny-udeb
nic-modules-${kernel:Version}-di
isa-pnp-modules-${kernel:Version}-di
modutils-basic
