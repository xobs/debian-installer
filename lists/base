# These udebs will be needed on every debian-installer system.
rootskel
anna
main-menu
cdebconf-udeb
udpkg
busybox-udeb
ash-udeb
kernel-image-${kernel:Version}-${kernel:Flavour}-udeb
# The shell doesn't work in a non-text debconf frontend
# And there's a shell on tty2, so commented it out
#di-utils-shell
